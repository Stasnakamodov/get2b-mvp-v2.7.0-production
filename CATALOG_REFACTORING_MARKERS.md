# 🏷️ СИСТЕМА МАРКЕРОВ ДЛЯ БЕЗОПАСНОГО РЕФАКТОРИНГА КАТАЛОГА

## 📌 ЦЕЛЬ РАЗМЕТКИ

Разметить монолитный файл `app/dashboard/catalog/page.tsx` (5436 строк) специальными маркерами для:
- ✅ Безопасного разделения на модули
- ✅ Сохранения работоспособности при рефакторинге
- ✅ Отслеживания зависимостей
- ✅ Предотвращения поломок критического функционала

---

## 🎨 СИСТЕМА МАРКЕРОВ

### 1. МАРКЕРЫ КРИТИЧНОСТИ

```typescript
// 🔴 CRITICAL: [описание] - НЕ ТРОГАТЬ без полного понимания
// 🟡 CAUTION: [описание] - Можно менять осторожно с тестами
// 🟢 SAFE: [описание] - Безопасно для рефакторинга
// ⚠️ DEPRECATED: [описание] - Устаревший код для удаления
```

### 2. МАРКЕРЫ МОДУЛЕЙ

```typescript
// 📦 MODULE_START: [ModuleName] - Начало логического модуля
// 📦 MODULE_END: [ModuleName] - Конец логического модуля
// 🔗 MODULE_DEPENDENCY: [ModuleName -> DependencyName] - Зависимость
```

### 3. МАРКЕРЫ РЕФАКТОРИНГА

```typescript
// 🔄 REFACTOR_TODO: [описание] - Требует рефакторинга
// 🐛 BUG_RISK: [описание] - Потенциальная ошибка
// 💡 OPTIMIZE: [описание] - Можно оптимизировать
// 🗑️ REMOVE_AFTER: [дата/версия] - Удалить после
```

### 4. МАРКЕРЫ ДАННЫХ

```typescript
// 📊 STATE_CRITICAL: [stateName] - Критическое состояние
// 🔐 AUTH_REQUIRED: [описание] - Требует авторизации
// 💾 DB_OPERATION: [таблица] - Операция с БД
// 🌐 API_CALL: [endpoint] - Вызов API
```

---

## 📋 ПЛАН РАЗМЕТКИ

### Фаза 1: Анализ структуры (строки 1-5436)

```typescript
// Основные секции файла page.tsx:
// 1-100: Импорты и типы
// 100-500: Константы и утилиты
// 500-1500: Модальные окна
// 1500-2500: Основной компонент CatalogPageClient
// 2500-3500: Обработчики событий
// 3500-4500: Рендер компонентов
// 4500-5436: Вспомогательные функции
```

### Фаза 2: Разметка критических функций

#### КРИТИЧЕСКИЕ ФУНКЦИИ (не ломать!):
```typescript
// 🔴 CRITICAL: loadSuppliers() - Загрузка поставщиков
// 🔴 CRITICAL: loadCategories() - Загрузка категорий
// 🔴 CRITICAL: handleAddSupplier() - Добавление поставщика
// 🔴 CRITICAL: handleImportFromCatalog() - Импорт из каталога
```

#### БЕЗОПАСНЫЕ ДЛЯ ВЫНОСА:
```typescript
// 🟢 SAFE: SupplierCard - Можно вынести в отдельный компонент
// 🟢 SAFE: FilterPanel - Можно вынести в отдельный компонент
// 🟢 SAFE: SortOptions - Можно вынести в отдельный компонент
```

### Фаза 3: Маркировка зависимостей

```typescript
// 🔗 MODULE_DEPENDENCY: CatalogPageClient -> supabase
// 🔗 MODULE_DEPENDENCY: CatalogPageClient -> ProductGridByCategory
// 🔗 MODULE_DEPENDENCY: loadSuppliers -> auth.token
// 🔗 MODULE_DEPENDENCY: handleAddSupplier -> toast
```

---

## 🛠️ ПРИМЕР РАЗМЕТКИ КОДА

```typescript
// 📦 MODULE_START: SupplierManagement
// 🔴 CRITICAL: Управление поставщиками - core функционал

  // 📊 STATE_CRITICAL: suppliers
  const [suppliers, setSuppliers] = useState<Supplier[]>([])

  // 🌐 API_CALL: /api/catalog/suppliers
  // 🔐 AUTH_REQUIRED: Требует токен авторизации
  // 🔄 REFACTOR_TODO: Вынести в хук useSuppliers
  const loadSuppliers = async () => {
    // 🐛 BUG_RISK: Нет обработки ошибок сети
    const response = await fetch('/api/catalog/suppliers', {
      headers: { Authorization: `Bearer ${token}` }
    })
    // 💡 OPTIMIZE: Добавить кеширование
    const data = await response.json()
    setSuppliers(data)
  }

// 📦 MODULE_END: SupplierManagement
```

---

## 📍 КОНКРЕТНЫЕ МЕСТА ДЛЯ РАЗМЕТКИ

### app/dashboard/catalog/page.tsx

| Строки | Маркер | Описание | Приоритет |
|--------|--------|----------|-----------|
| 1-50 | 🟢 SAFE | Импорты | Низкий |
| 100-200 | 🔴 CRITICAL | Типы данных | Высокий |
| 500-800 | 🟡 CAUTION | Модальные окна | Средний |
| 1500-1600 | 🔴 CRITICAL | Инициализация состояний | Высокий |
| 1800-2000 | 🔄 REFACTOR_TODO | loadSuppliers, loadCategories | Высокий |
| 2254-2262 | 🐛 BUG_RISK | ProductGridByCategory без подкатегории | Критический |
| 3000-3500 | 🟢 SAFE | UI компоненты | Низкий |
| 4000-4500 | 💡 OPTIMIZE | Фильтрация и сортировка | Средний |

### components/catalog/ProductGridByCategory.tsx

| Строки | Маркер | Описание | Приоритет |
|--------|--------|----------|-----------|
| 62-71 | 🐛 BUG_RISK | Interface без selectedSubcategory | Критический |
| 95-104 | 🐛 BUG_RISK | Props без selectedSubcategory | Критический |
| 115-118 | 🐛 BUG_RISK | useEffect без зависимости | Критический |
| 126-145 | 🔄 REFACTOR_TODO | loadProducts использует только категорию | Высокий |

---

## ✅ ЧЕК-ЛИСТ БЕЗОПАСНОГО РЕФАКТОРИНГА

### Перед началом:
- [ ] Создать бэкап текущего кода
- [ ] Запустить тесты (если есть)
- [ ] Задокументировать текущее поведение
- [ ] Разметить все критические функции

### Во время рефакторинга:
- [ ] Работать с одним модулем за раз
- [ ] Не трогать 🔴 CRITICAL без необходимости
- [ ] Тестировать после каждого изменения
- [ ] Коммитить малыми порциями

### После модуля:
- [ ] Проверить все 🔗 MODULE_DEPENDENCY
- [ ] Запустить интеграционные тесты
- [ ] Обновить маркеры
- [ ] Задокументировать изменения

---

## 🎯 ПРИОРИТЕТЫ РЕФАКТОРИНГА

### Приоритет 1: Исправить баги
1. ❗ Передача selectedSubcategory в ProductGridByCategory
2. ❗ Добавление зависимостей в useEffect
3. ❗ Обработка ошибок в API вызовах

### Приоритет 2: Вынести в компоненты
1. SupplierCard → components/catalog/SupplierCard.tsx
2. FilterPanel → components/catalog/FilterPanel.tsx
3. CategorySelector → components/catalog/CategorySelector.tsx

### Приоритет 3: Создать хуки
1. useSuppliers → hooks/useSuppliers.ts
2. useCategories → hooks/useCategories.ts
3. useProducts → hooks/useProducts.ts

### Приоритет 4: Сервисный слой
1. CatalogService → services/catalogService.ts
2. SupplierService → services/supplierService.ts
3. ProductService → services/productService.ts

---

## 📝 КОМАНДЫ ДЛЯ АВТОМАТИЧЕСКОЙ РАЗМЕТКИ

```bash
# Найти все функции больше 50 строк
grep -n "^[[:space:]]*const.*=.*async.*{$" page.tsx

# Найти все useState
grep -n "useState" page.tsx

# Найти все API вызовы
grep -n "fetch(" page.tsx

# Найти все обработчики
grep -n "handle[A-Z]" page.tsx

# Подсчитать количество компонентов
grep -c "return (" page.tsx
```

---

## 🔒 ПРАВИЛА БЕЗОПАСНОСТИ

1. **НЕ УДАЛЯТЬ** код с маркером 🔴 CRITICAL без анализа зависимостей
2. **НЕ МЕНЯТЬ** типы данных без проверки всех использований
3. **НЕ ПЕРЕНОСИТЬ** функции без их зависимостей
4. **ВСЕГДА** тестировать после изменения критических функций
5. **ДОКУМЕНТИРОВАТЬ** все неочевидные решения

---

## 💾 СОХРАНЕНИЕ ПРОГРЕССА

После каждого этапа рефакторинга:
1. Обновить этот документ
2. Отметить выполненные задачи
3. Добавить новые находки
4. Зафиксировать проблемы

**Последнее обновление:** [дата]
**Прогресс:** 0/5436 строк размечено
**Модулей выделено:** 0
**Багов исправлено:** 0