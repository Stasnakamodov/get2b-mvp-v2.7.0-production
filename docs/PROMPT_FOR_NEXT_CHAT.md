# ПРОМПТ ДЛЯ НОВОГО ЧАТА - ВЫПОЛНЕНИЕ МИГРАЦИИ КАТЕГОРИЙ

Скопируй этот промпт целиком и вставь в новый чат с Claude Code:

---

Привет! Мне нужна помощь с выполнением миграции иерархической системы категорий в проекте Get2B.

**Контекст:**
Был проведён полный аудит плана миграции категорий. Старый план оказался опасным (риск 80-90%), был создан новый безопасный план v2.0 (риск 12%).

**Текущая ситуация БД:**
- `catalog_categories`: 8 основных категорий (уже существует!)
- `catalog_subcategories`: 0 записей (пустая)
- `catalog_products`: 70 товаров с `category_id = NULL` (не привязаны)
- `catalog_suppliers`: 10 поставщиков с `category_id = NULL` (не привязаны)
- `supplier_profiles`: 0 записей
- `client_profiles`: 4 записи

**Что нужно сделать:**
Выполнить безопасную миграцию согласно плану v2.0:

1. **Расширить схему** `catalog_categories` (добавить parent_id, level, full_path и др.)
2. **Создать триггеры** для автоматического обновления счётчиков
3. **Заполнить подкатегории** (~32 шт для 8 основных категорий)
4. **Категоризировать products** (привязать 70 товаров к категориям)
5. **Категоризировать suppliers** (привязать 10 поставщиков)
6. **Исправить supplier_profiles** (добавить category_id uuid)
7. **Создать SQL функцию** для получения дерева категорий
8. **Обновить API** endpoints для работы с иерархией
9. **Протестировать** всё через SQL тесты

**Документация подготовлена:**
- ✅ `docs/AUDIT_SUMMARY.md` - краткий итог аудита
- ✅ `docs/CATEGORY_MIGRATION_PLAN_V2_SAFE.md` - полный план миграции
- ✅ `docs/MIGRATION_EXECUTION_GUIDE.md` - пошаговая инструкция
- ✅ `supabase/migrations-v2-safe/20251030_100_extend_catalog_categories.sql` - первая миграция
- ⚠️ Остальные миграции нужно создать по образцу из плана

**MCP серверы доступны:**
- ✅ MCP Supabase - для работы с БД
- ✅ MCP Context7 - для анализа кодабазы

**Проект:**
- Next.js 15 + TypeScript
- Supabase (Postgres)
- Директория: `/Users/user/Desktop/godplisgomvp-forvercel`

---

## ЧТО ОТ ТЕБЯ НУЖНО:

### ШАГ 1: Ознакомиться с контекстом
Прочитай файлы:
1. `docs/AUDIT_SUMMARY.md` - чтобы понять проблему
2. `docs/CATEGORY_MIGRATION_PLAN_V2_SAFE.md` - чтобы понять решение
3. `docs/MIGRATION_EXECUTION_GUIDE.md` - чтобы понять процесс

### ШАГ 2: Создать оставшиеся миграции
Создай файлы в `supabase/migrations-v2-safe/`:
- `20251030_101_create_category_triggers.sql`
- `20251030_102_seed_subcategories.sql`
- `20251030_103_categorize_products.sql`
- `20251030_104_categorize_suppliers.sql`
- `20251030_105_fix_supplier_profiles.sql`
- `20251030_106_create_tree_function.sql`

Код для них есть в `CATEGORY_MIGRATION_PLAN_V2_SAFE.md` - просто извлеки в отдельные файлы.

### ШАГ 3: Создать SQL тесты
Создай файл `supabase/tests/test_category_migrations.sql` - код тоже есть в плане.

### ШАГ 4: Сделать backup БД
Через MCP Supabase или через команду:
```bash
PGPASSWORD="B2ryf4elLIDqghCR" pg_dump \
  -h aws-0-us-east-1.pooler.supabase.com \
  -p 6543 \
  -U postgres.ejkhdhexkadecpbjjmsz \
  -d postgres \
  -Fc \
  -f backup_$(date +%Y%m%d_%H%M%S).dump
```

### ШАГ 5: Проверить текущее состояние БД
Через MCP Supabase выполни:
```sql
SELECT
  (SELECT COUNT(*) FROM catalog_categories) as categories,
  (SELECT COUNT(*) FROM catalog_products) as products,
  (SELECT COUNT(*) FROM catalog_suppliers) as suppliers;
```

Ожидаемый результат: categories=8, products=70, suppliers=10

### ШАГ 6: Выполнить миграции по очереди
Используя MCP Supabase:

1. Применить `20251030_100_extend_catalog_categories.sql`
2. Проверить что колонки добавились
3. Применить `20251030_101_create_category_triggers.sql`
4. Проверить что триггеры созданы
5. Применить `20251030_102_seed_subcategories.sql`
6. Проверить что подкатегории добавились (должно быть ~32)
7. Применить `20251030_103_categorize_products.sql`
8. Проверить что products привязались к категориям
9. Применить `20251030_104_categorize_suppliers.sql`
10. Проверить что suppliers привязались
11. Применить `20251030_105_fix_supplier_profiles.sql`
12. Применить `20251030_106_create_tree_function.sql`

После КАЖДОЙ миграции делай checkpoint - проверяй что всё корректно!

### ШАГ 7: Запустить тесты
Выполнить `supabase/tests/test_category_migrations.sql`

Ожидаемый результат:
```
✅ Тест 1: Структура catalog_categories корректна
✅ Тест 2: Основных категорий: 8
✅ Тест 3: Подкатегорий: 32
✅ Тест 4: Все parent_id валидны
✅ Тест 5: Все категории имеют full_path
✅ Тест 6: Триггеры работают корректно
✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!
```

### ШАГ 8: Обновить API endpoint
Обновить файл `app/api/catalog/categories/route.ts` согласно коду из плана (раздел "ЭТАП 5: Обновление API").

Создать новый файл `app/api/catalog/category-tree/route.ts` - код тоже в плане.

### ШАГ 9: Протестировать API
Запустить dev сервер и проверить:
```bash
npm run dev

# В другом терминале:
curl http://localhost:3000/api/catalog/categories | jq .
```

Ожидаемый ответ должен содержать:
- `categories`: массив
- `categoryTree`: массив с children
- `stats.total`: >= 40

### ШАГ 10: Финальный отчёт
Предоставь мне:
1. ✅/❌ Статус каждой миграции
2. Количество категорий по уровням (0/1/2)
3. Сколько products/suppliers категоризировано
4. Результаты тестов
5. Скриншот ответа API (или JSON)

---

## ВАЖНЫЕ ПРАВИЛА:

1. **ОБЯЗАТЕЛЬНО сделай backup** перед началом!
2. **Используй MCP Supabase** для выполнения SQL (НЕ через psql напрямую)
3. **Проверяй результат ПОСЛЕ КАЖДОЙ миграции** - не иди дальше если что-то не так
4. **Если ошибка** - ОСТАНОВИ выполнение, сообщи мне, НЕ продолжай
5. **Используй транзакции** для безопасности (BEGIN/COMMIT/ROLLBACK)

---

## ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК:

### Откат:
```sql
-- Восстановить из backup
-- ИЛИ
BEGIN;
DELETE FROM catalog_categories WHERE level > 0;
ALTER TABLE catalog_categories
  DROP COLUMN IF EXISTS parent_id,
  DROP COLUMN IF EXISTS level,
  DROP COLUMN IF EXISTS full_path,
  DROP COLUMN IF EXISTS products_count,
  DROP COLUMN IF EXISTS suppliers_count,
  DROP COLUMN IF EXISTS is_popular,
  DROP COLUMN IF EXISTS metadata,
  DROP COLUMN IF EXISTS slug;
COMMIT;
```

---

## ОЖИДАЕМОЕ ВРЕМЯ:

- Создание файлов миграций: 15 минут
- Backup: 5 минут
- Выполнение миграций: 30 минут
- Тестирование: 15 минут
- Обновление API: 30 минут
- Финальная проверка: 15 минут

**Итого:** ~2 часа

---

## НАЧИНАЙ!

Подтверди что:
1. Прочитал всю документацию
2. Понял план действий
3. Готов создавать миграции
4. Будешь использовать MCP Supabase
5. Сделаешь backup перед началом

После подтверждения - приступай к ШАГ 1! 🚀

---

## КОНТЕКСТ ДЛЯ MCP:

**БД Connection:**
- Host: aws-0-us-east-1.pooler.supabase.com
- Port: 6543
- User: postgres.ejkhdhexkadecpbjjmsz
- Database: postgres
- Password: B2ryf4elLIDqghCR

**Текущие категории в БД (8 шт):**
1. Электроника (electronics)
2. Автотовары (automotive)
3. Промышленность (industrial)
4. Здоровье и медицина (healthcare)
5. Текстиль и одежда (textiles)
6. Строительство (construction)
7. Продукты питания (food)
8. Дом и быт (home)

**Директория проекта:**
`/Users/user/Desktop/godplisgomvp-forvercel`

Удачи! Я жду твоего отчёта! 💪
