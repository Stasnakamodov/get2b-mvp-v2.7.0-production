# КОНТЕКСТ И ЗАДАЧА: Исправление корзины в B2B каталоге

## 🎯 ГЛАВНАЯ ПРОБЛЕМА
Корзина работает плохо! После добавления товара в корзину:
1. **НЕ показываются другие товары этого же поставщика**
2. Блокируются ВСЕ товары других поставщиков (становятся "Недоступно")
3. Нет рекомендаций других товаров от активного поставщика
4. Плохой UX - пользователь не понимает, что можно добавлять только от одного поставщика

## 📁 СТРУКТУРА ПРОЕКТА
```
/Users/user/Desktop/godplisgomvp-forvercel/
├── app/dashboard/catalog/page.tsx          # Главная страница каталога
├── components/catalog/
│   └── ProductGridByCategory.tsx          # Компонент отображения товаров
├── src/features/cart-management/
│   └── hooks/useCart.ts                   # Хук управления корзиной
└── src/entities/product/                  # Типы товаров
```

## 🔍 КАК СЕЙЧАС РАБОТАЕТ КОРЗИНА

### 1. Логика блокировки поставщиков (`useCart.ts`, строки 82-111):
```typescript
const addToCart = (product: Product, quantity: number = 1): boolean => {
  const supplierId = product.supplier_id

  // Если корзина пустая или тот же поставщик
  if (cart.length === 0 || activeSupplier === supplierId) {
    setActiveSupplier(supplierId)
    // Добавляем товар...
    return true
  } else {
    // Блокируем товары других поставщиков
    return false
  }
}
```

### 2. В интерфейсе (`ProductGridByCategory.tsx`, строка 661):
```typescript
isDisabled={activeSupplier !== null &&
  product.supplier_name !== activeSupplier &&
  product.supplier_company_name !== activeSupplier &&
  !isProductInCart(product.id)}
```

### 3. Индикатор активного поставщика (строки 409-421):
```tsx
{activeSupplier && (
  <div className="p-2 bg-blue-50 rounded-lg">
    <span>Показаны товары от: <strong>{activeSupplier}</strong></span>
    <span>Очистите корзину для выбора другого поставщика</span>
  </div>
)}
```

## ❌ ЧТО НЕ РАБОТАЕТ

1. **Нет фильтрации по активному поставщику**
   - После добавления товара в корзину должны показываться ТОЛЬКО товары этого поставщика
   - Сейчас показываются все товары, но другие заблокированы

2. **Плохая визуализация**
   - Заблокированные товары выглядят как сломанные
   - Нет объяснения почему они недоступны

3. **Нет рекомендаций**
   - Не предлагаются другие товары активного поставщика
   - Пользователь не видит весь ассортимент поставщика

## ✅ ЧТО НУЖНО СДЕЛАТЬ

### Вариант 1: Автоматическая фильтрация (РЕКОМЕНДУЕТСЯ)
После добавления товара в корзину:
1. Автоматически фильтровать товары - показывать ТОЛЬКО от активного поставщика
2. Добавить кнопку "Показать всех поставщиков" с предупреждением об очистке корзины
3. Добавить счетчик "Еще N товаров от этого поставщика"

### Вариант 2: Умная группировка
1. Сгруппировать товары по поставщикам
2. Выделить секцию активного поставщика
3. Остальные секции сделать полупрозрачными с объяснением

### Вариант 3: Модальное окно рекомендаций
После добавления товара показывать:
1. "Товар добавлен! Посмотрите другие товары от [Название поставщика]"
2. Карусель с товарами этого поставщика
3. Кнопку "Продолжить покупки у этого поставщика"

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

**Текущий стек:**
- Next.js 15, React 18, TypeScript
- Tailwind CSS
- FSD архитектура
- Supabase для данных

**API эндпоинт товаров:**
`/api/catalog/products-by-category?supplier_id=XXX`
- Можно фильтровать по supplier_id

**Важные поля товара:**
- `supplier_id` - ID поставщика
- `supplier_name` - Имя поставщика
- `supplier_company_name` - Компания поставщика

## 📝 ДОПОЛНИТЕЛЬНЫЙ КОНТЕКСТ

Пользователь жалуется: "бля корзина очень плохо работает другие товары поставщика мне не предложило - функции сломан"

Это B2B платформа где важно:
- Заказывать у одного поставщика за раз (логистика)
- Видеть весь ассортимент выбранного поставщика
- Понимать ограничения и почему они существуют

## 🎯 ИТОГОВАЯ ЗАДАЧА

**Исправить корзину так, чтобы:**
1. После добавления товара автоматически показывались другие товары ЭТОГО ЖЕ поставщика
2. Было понятно, почему заблокированы товары других поставщиков
3. Легко было найти и добавить другие товары активного поставщика
4. UX был понятным и приятным

**Приоритет:** Вариант 1 - автоматическая фильтрация по активному поставщику