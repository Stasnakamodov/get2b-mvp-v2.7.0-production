# ✅ ИСПРАВЛЕНИЕ ОТОБРАЖЕНИЯ ТОВАРОВ В ПОДКАТЕГОРИЯХ

## 📅 Дата: 29.11.2025

## 🔴 Проблема
Категория "ТЕСТОВАЯ" показывала 111 товаров в счетчике, но при открытии подкатегории "Тестовые товары" отображалось "0 товаров найдено".

## 🔍 Анализ проблемы

### 1. Структура данных в БД:
- **Товары хранятся с полем category = "ТЕСТОВАЯ"**
- В спецификациях товаров есть поле subcategory со значениями: "Смартфоны", "Ноутбуки", "Наушники" и т.д.

### 2. Структура категорий в UI:
```json
{
  "name": "ТЕСТОВАЯ",
  "subcategories": [
    {
      "name": "Тестовые товары",
      "key": "test_products"
    }
  ]
}
```

### 3. Причина проблемы:
- При выборе подкатегории "Тестовые товары" в компонент ProductGridByCategory передавалось имя подкатегории ("Тестовые товары")
- API искал товары WHERE category = "Тестовые товары"
- В базе нет товаров с категорией "Тестовые товары", все товары имеют категорию "ТЕСТОВАЯ"
- Результат: 0 товаров

## ✅ Решение

### Файл: `app/dashboard/catalog/page.tsx:2256`

**БЫЛО:**
```typescript
<ProductGridByCategory
  selectedCategory={selectedSubcategoryData?.name || selectedSubcategoryData?.category || ''}
  // При выборе подкатегории передавалось "Тестовые товары"
```

**СТАЛО:**
```typescript
<ProductGridByCategory
  selectedCategory={selectedCategoryData?.name || ''}
  // Теперь всегда передается имя родительской категории "ТЕСТОВАЯ"
```

## 📊 Результат

### До исправления:
- Категория показывает: 111 товаров
- При открытии подкатегории: 0 товаров

### После исправления:
- Категория показывает: 111 товаров
- При открытии подкатегории: 111 товаров отображаются корректно

## ⚠️ Важное замечание

Текущее решение показывает **ВСЕ товары категории** при выборе любой подкатегории, так как:
1. Подкатегория в UI называется "Тестовые товары"
2. Товары в БД имеют подкатегории: "Смартфоны", "Ноутбуки", "Наушники" и т.д.
3. Нет соответствия между названиями подкатегорий

## 🚀 Рекомендации для полноценной работы подкатегорий

### Вариант 1: Синхронизация подкатегорий
Создать правильные подкатегории в таблице catalog_subcategories, соответствующие значениям в товарах:
```sql
-- Добавить реальные подкатегории
INSERT INTO catalog_subcategories (category_id, name, key) VALUES
  ('a3bb6211-4c81-44c6-a328-42092b27234b', 'Смартфоны', 'smartphones'),
  ('a3bb6211-4c81-44c6-a328-42092b27234b', 'Ноутбуки', 'laptops'),
  ('a3bb6211-4c81-44c6-a328-42092b27234b', 'Наушники', 'headphones');
```

### Вариант 2: Добавить фильтрацию по подкатегории
Модифицировать ProductGridByCategory для фильтрации по subcategory из specifications:
```typescript
// Добавить новый prop subcategory
interface ProductGridByCategoryProps {
  selectedCategory: string
  selectedSubcategory?: string
  // ...
}

// Фильтровать товары по подкатегории
if (selectedSubcategory) {
  filtered = filtered.filter(product =>
    product.specifications?.subcategory === selectedSubcategory
  )
}
```

## ✅ Текущий статус

**ПРОБЛЕМА РЕШЕНА** - товары теперь отображаются при открытии категории "ТЕСТОВАЯ".

Для полноценной работы с подкатегориями требуется дополнительная доработка согласно рекомендациям выше.

---

*Исправление применено и протестировано. Товары успешно отображаются в каталоге.*