# Настройка YandexGPT для умного поиска по изображению

## Что дает YandexGPT?

YandexGPT улучшает поиск товаров по изображению на **40-60%** за счет:
- Определения бренда товара (Brembo, Samsung, Bosch и т.д.)
- Автоматической генерации синонимов (brake → тормоз, суппорт, тормозная система)
- Добавления ключевых слов на русском и английском языках
- Умного анализа контекста изображения

## Текущая работа системы

### Без YandexGPT (Fallback режим):
```
Изображение Brembo brake disc
↓
OCR: "Brembo", "GT"
Classification: "brake", "disc", "automotive"
↓
Поиск: 5 ключевых слов
```

### С YandexGPT:
```
Изображение Brembo brake disc
↓
OCR: "Brembo", "GT"
Classification: "brake", "disc", "automotive"
YandexGPT: "Брембо", "тормоза", "суппорт", "тормозная система", "brake system"
↓
Поиск: 15 ключевых слов
```

## Как подключить YandexGPT?

### Шаг 1: Создать API ключ для Foundation Models

1. Откройте [Yandex Cloud Console](https://console.cloud.yandex.ru/)
2. Выберите ваш каталог (Folder ID уже используется для Yandex Vision)
3. Перейдите в раздел **"Service accounts"** (Сервисные аккаунты)
4. Выберите существующий сервисный аккаунт или создайте новый
5. Назначьте роль: **`ai.languageModels.user`**
   - Это даст доступ к Foundation Models (YandexGPT)
6. Создайте API ключ:
   - Нажмите на сервисный аккаунт
   - Вкладка **"API keys"**
   - **"Create API key"**
   - Скопируйте ключ (показывается только один раз!)

### Шаг 2: Добавить переменную окружения

Откройте файл `.env.local` и добавьте:

```bash
# Можно использовать тот же API ключ, что и для Yandex Vision
YANDEX_GPT_API_KEY=AQVNxxxxxxxxxxxxxxxxxxxxxxxxx

# Или создать отдельный (опционально)
# YANDEX_VISION_API_KEY=AQVNxxxxxxxxxxxxxxxxxxxxxxxxx
# YANDEX_GPT_API_KEY=AQVNyyyyyyyyyyyyyyyyyyyyyyyyy

# Folder ID (уже должен быть настроен)
YANDEX_FOLDER_ID=b1gxxxxxxxxxxxxxxxxxxxx
```

**Важно:**
- Если `YANDEX_GPT_API_KEY` не указан, система автоматически использует `YANDEX_VISION_API_KEY`
- Можно использовать один и тот же API ключ для обоих сервисов

### Шаг 3: Перезапустить сервер

```bash
# Останови текущий сервер (Ctrl+C)
npm run dev
```

### Шаг 4: Проверить работу

Откройте консоль браузера (F12) и попробуйте загрузить изображение товара через кнопку камеры.

Вы увидите логи:
```
🤖 YandexGPT: Проверяем переменные окружения...
🔑 API Key: AQVNxxxx...
📁 Folder ID: b1gxxxxxx
🤖 [IMAGE SEARCH] Запускаем YandexGPT для умного анализа...
🎯 [IMAGE SEARCH] YandexGPT результат: {brand: "Brembo", category: "Автозапчасти", ...}
```

Если ключ не настроен:
```
⚠️ YandexGPT: Используем fallback режим (OCR + Classification)
```

## Стоимость использования

### YandexGPT Foundation Models:
- **~1₽ за 1000 токенов** (вход + выход)
- Один запрос поиска по изображению: **~200-500 токенов**
- **Стоимость одного поиска: ~0.3-0.5₽**

### Пример расчета:
- 100 поисков по изображению в день = **30-50₽/день**
- 1000 поисков в месяц = **300-500₽/месяц**

**Очень дешево!** 🎉

### Для сравнения:
- OpenAI GPT-4 Vision: **$0.01 за изображение (~1₽)**
- Google Cloud Vision: **$1.50 за 1000 запросов**
- Yandex Vision (OCR + Classification): **~480₽ за 1 млн символов**

## Что делать, если не хочешь подключать YandexGPT?

Ничего! Система работает в **fallback режиме** без YandexGPT:
- Используется OCR + Classification
- Точность поиска ниже (~60% от максимальной)
- Но все равно работает!

## Лимиты и квоты

### YandexGPT Foundation Models:
- **10 запросов в секунду** (достаточно для большинства случаев)
- **Без ограничения на количество запросов в день**

### Как увеличить лимиты:
1. Откройте [Yandex Cloud Console](https://console.cloud.yandex.ru/)
2. Перейдите в **"Quotas"** (Квоты)
3. Найдите **"Foundation Models"**
4. Нажмите **"Request increase"** (Запросить увеличение)

## Troubleshooting

### Ошибка: "Yandex Vision API не настроен"
```
🤖 YandexGPT: Проверяем переменные окружения...
🔑 API Key: НЕ НАЙДЕН
📁 Folder ID: НЕ НАЙДЕН
```

**Решение:** Проверьте `.env.local`:
- `YANDEX_GPT_API_KEY` или `YANDEX_VISION_API_KEY` должен быть указан
- `YANDEX_FOLDER_ID` должен быть указан
- Перезапустите сервер после изменения `.env.local`

### Ошибка: "403 Forbidden"
```
❌ YandexGPT API ошибка: 403 - Permission denied
```

**Решение:** Проверьте роль сервисного аккаунта:
- Должна быть роль `ai.languageModels.user`
- Или роль `editor`/`admin` для каталога

### Ошибка: "429 Too Many Requests"
```
❌ YandexGPT API ошибка: 429 - Rate limit exceeded
```

**Решение:**
- Вы превысили лимит 10 запросов в секунду
- Запросите увеличение квоты в Yandex Cloud Console
- Или добавьте rate limiting в код

### YandexGPT не улучшает результаты
```
✅ [IMAGE SEARCH] Найдено товаров: 0
```

**Возможные причины:**
1. В каталоге нет товаров с такими ключевыми словами
2. Изображение плохого качества (размытое, темное)
3. Текст на изображении нечитаемый

**Решение:**
- Добавьте товары в каталог с правильными названиями и описаниями
- Используйте качественные фото с четким текстом
- Проверьте логи в консоли браузера

## Мониторинг использования

### Просмотр статистики:
1. Откройте [Yandex Cloud Console](https://console.cloud.yandex.ru/)
2. Перейдите в **"Monitoring"** (Мониторинг)
3. Выберите **"Foundation Models"**
4. Смотрите графики:
   - Количество запросов
   - Средняя стоимость
   - Ошибки

### Логи в коде:
Все логи YandexGPT начинаются с `🤖`:
```javascript
console.log('🤖 YandexGPT: начинаем умный анализ...')
console.log('🎯 YandexGPT результат:', analysis)
```

## Примеры использования

### Пример 1: Тормозной диск Brembo
**Входные данные:**
- OCR: "Brembo GT"
- Classification: "brake", "disc", "automotive"

**YandexGPT добавляет:**
```json
{
  "brand": "Brembo",
  "category": "Автозапчасти",
  "keywords": [
    "Brembo",
    "Брембо",
    "тормоза",
    "brake",
    "суппорт",
    "тормозная система",
    "GT",
    "disc",
    "диск"
  ]
}
```

### Пример 2: Samsung холодильник
**Входные данные:**
- OCR: "Samsung RB37"
- Classification: "appliance", "refrigerator"

**YandexGPT добавляет:**
```json
{
  "brand": "Samsung",
  "category": "Бытовая техника",
  "keywords": [
    "Samsung",
    "Самсунг",
    "холодильник",
    "refrigerator",
    "RB37",
    "бытовая техника",
    "appliance"
  ]
}
```

## Альтернативные решения

Если YandexGPT не подходит, можно использовать:

### 1. Yandex Translator API
```typescript
// Только перевод терминов
"brake" → "тормоз"
"disc" → "диск"
```
- Стоимость: ~480₽ за 1 млн символов
- Улучшение: +20%

### 2. OpenAI GPT-4 Vision
```typescript
// Очень умный, но дорогой
```
- Стоимость: $0.01 за изображение (~1₽)
- Улучшение: +50-70%
- Требует VPN

### 3. Google Cloud Vision
```typescript
// Хороший для известных брендов
```
- Стоимость: $1.50 за 1000 запросов
- Улучшение: +40-50%
- Требует карту

## Поддержка

Если есть проблемы:
1. Проверьте логи в консоли браузера (F12)
2. Проверьте логи сервера (`npm run dev`)
3. Проверьте `.env.local`
4. Создайте issue в репозитории

---

**Итог:** YandexGPT - самое дешевое и эффективное решение для умного поиска по изображению! 🚀
