# 🎯 ФАЗА 2: ИЗВЛЕЧЕНИЕ ДАННЫХ ИЗ LANDING PAGE

## КОНТЕКСТ

**Фаза 1 завершена ✅**. Создана полная инфраструктура для декомпозиции:
- Структура папок (components/landing, data/landing, hooks/landing, constants, types)
- TypeScript типы (117 строк в types/landing.ts)
- Константы (gradients.ts, animations.ts)
- Заглушки для данных, утилит и hooks с TODO комментариями

Файл `/Users/user/Desktop/godplisgomvp-forvercel/app/page.tsx` остается нетронутым и работает.

Это **ФАЗА 2: Извлечение данных** (оценка: 1.5 часа).

---

## ЦЕЛЬ ФАЗЫ 2

Извлечь все статические данные из `app/page.tsx` в соответствующие файлы в `/data/landing/`. НЕ изменять сам `app/page.tsx` - только копировать данные.

---

## ВАЖНО ⚠️

1. **НЕ ТРОГАТЬ `app/page.tsx`** - он должен продолжать работать!
2. Только **копировать данные** из него в новые файлы
3. Все данные должны быть **типизированы** согласно `types/landing.ts`
4. После заполнения удалить `TODO` комментарии из файлов
5. Проверить что TypeScript компилируется без ошибок

---

## ЗАДАЧИ

### 1. ЗАПОЛНИТЬ `/data/landing/steps.ts` (20 минут)

**Что извлечь из `app/page.tsx`:**
- Найди массив с 7 шагами процесса закупки (шаг I-VII)
- Каждый шаг содержит: number (римская цифра), title, description, time, icon

**Структура:**

```typescript
import type { ProcessStep } from '@/types/landing'
import {
  Building2,
  ShoppingCart,
  DollarSign,
  FileCheck,
  FileText,
  ArrowRightLeft,
  Truck,
} from 'lucide-react'

/**
 * 7 шагов процесса закупки Get2B
 */
export const processSteps: ProcessStep[] = [
  {
    number: 'I',
    title: 'Название шага 1',
    description: 'Описание шага 1...',
    time: '10 мин',
    icon: Building2,
  },
  // ... остальные 6 шагов
]
```

**Проверка:**
- Все 7 шагов извлечены
- Каждый шаг типизирован как `ProcessStep`
- Иконки импортированы из `lucide-react`
- Убран `TODO` комментарий

---

### 2. ЗАПОЛНИТЬ `/data/landing/benefits.ts` (15 минут)

**Что извлечь из `app/page.tsx`:**
- Найди секцию с преимуществами Get2B
- Обычно 6-8 карточек с иконкой, заголовком и описанием

**Структура:**

```typescript
import type { Benefit } from '@/types/landing'
import {
  Shield,
  Zap,
  Users,
  BarChart,
  MessageCircle,
  TrendingUp,
} from 'lucide-react'

/**
 * Преимущества платформы Get2B
 */
export const benefits: Benefit[] = [
  {
    icon: Shield,
    title: 'Название преимущества 1',
    description: 'Описание преимущества 1...',
  },
  // ... остальные преимущества
]
```

**Проверка:**
- Все преимущества извлечены
- Каждое типизировано как `Benefit`
- Иконки корректно импортированы
- Убран `TODO` комментарий

---

### 3. ЗАПОЛНИТЬ `/data/landing/faq.ts` (15 минут)

**Что извлечь из `app/page.tsx`:**
- Найди секцию FAQ с вопросами и ответами
- Обычно 5-10 вопросов

**Структура:**

```typescript
import type { FAQItem } from '@/types/landing'

/**
 * Часто задаваемые вопросы (FAQ)
 */
export const faqItems: FAQItem[] = [
  {
    question: 'Вопрос 1?',
    answer: 'Ответ на вопрос 1...',
  },
  // ... остальные вопросы
]
```

**Проверка:**
- Все вопросы извлечены
- Каждый типизирован как `FAQItem`
- Форматирование сохранено
- Убран `TODO` комментарий

---

### 4. ЗАПОЛНИТЬ `/data/landing/tutorial.ts` (20 минут)

**Что извлечь из `app/page.tsx`:**
- Найди объект `tutorialContent` с контентом для модалок
- 5 типов: 'cart', 'globe', 'camera', 'new-project', 'catalog'
- Каждый содержит: title, description, features (массив), icon, color (градиент)

**Структура:**

```typescript
import type { TutorialContent, TutorialType } from '@/types/landing'
import {
  ShoppingCart,
  Globe,
  Camera,
  Plus,
} from 'lucide-react'

/**
 * Контент для интерактивных tutorial модалок
 */
export const tutorialContent: Record<TutorialType, TutorialContent> = {
  cart: {
    title: '🛒 Заголовок для корзины',
    description: 'Описание...',
    features: [
      'Фича 1',
      'Фича 2',
      // ... и т.д.
    ],
    icon: ShoppingCart,
    color: 'from-blue-500 to-blue-600',
  },
  globe: {
    // ... аналогично
  },
  camera: {
    // ... аналогично
  },
  'new-project': {
    // ... аналогично
  },
  catalog: {
    // ... аналогично
  },
}
```

**Проверка:**
- Все 5 типов tutorial заполнены
- Типизировано как `Record<TutorialType, TutorialContent>`
- Иконки импортированы
- Градиенты взяты из существующего кода
- Убран `TODO` комментарий

---

### 5. ЗАПОЛНИТЬ `/data/landing/mockData.ts` (20 минут)

**Что извлечь из `app/page.tsx`:**
- Найди `mockProjects` - mock проекты для dashboard preview
- Найди массив с template карточками ("Клиент", "Поставщик")

**Структура:**

```typescript
import type { Project, Template } from '@/types/landing'

/**
 * Mock проекты для preview dashboard
 * Показываются когда у пользователя нет реальных проектов
 */
export const mockProjects: Project[] = [
  {
    id: '1',
    name: 'Название проекта 1',
    company_data: {
      name: 'Компания 1',
      inn: '1234567890',
      // ... и т.д.
    },
    amount: 1000000,
    currency: 'RUB',
    created_at: '2024-01-15T10:00:00Z',
    status: 'active',
    current_step: 3,
    max_step_reached: 4,
    user_id: 'mock-user',
  },
  // ... остальные mock проекты (обычно 3-5 штук)
]

/**
 * Template карточки для выбора роли
 */
export const mockTemplates: Template[] = [
  {
    id: 'client',
    name: 'Клиент',
    description: 'Описание роли клиента...',
    role: 'client',
  },
  {
    id: 'supplier',
    name: 'Поставщик',
    description: 'Описание роли поставщика...',
    role: 'supplier',
  },
]
```

**Проверка:**
- Все mock проекты извлечены
- Все templates извлечены
- Типизированы как `Project[]` и `Template[]`
- Убран `TODO` комментарий

---

## ТЕСТИРОВАНИЕ ПОСЛЕ ФАЗЫ 2

После завершения всех задач:

### 1. Проверка TypeScript (5 минут)

```bash
npx tsc --noEmit
```

**Ожидаемый результат:** Новые файлы компилируются без ошибок

### 2. Проверка импортов (5 минут)

Создай тестовый файл `/test-phase-2.ts`:

```typescript
import { processSteps } from './data/landing/steps'
import { benefits } from './data/landing/benefits'
import { faqItems } from './data/landing/faq'
import { tutorialContent } from './data/landing/tutorial'
import { mockProjects, mockTemplates } from './data/landing/mockData'

console.log('✅ Process steps:', processSteps.length)
console.log('✅ Benefits:', benefits.length)
console.log('✅ FAQ items:', faqItems.length)
console.log('✅ Tutorial types:', Object.keys(tutorialContent).length)
console.log('✅ Mock projects:', mockProjects.length)
console.log('✅ Mock templates:', mockTemplates.length)
```

Запусти:
```bash
npx tsx test-phase-2.ts
```

**Ожидаемый результат:** Все импорты работают, выводятся корректные количества

### 3. Проверка что app/page.tsx работает (5 минут)

```bash
npm run dev
```

Открой http://localhost:3000 и убедись что:
- ✅ Лендинг загружается
- ✅ Все секции отображаются
- ✅ Dashboard preview работает
- ✅ Tutorial модалки открываются

---

## ЧЕКЛИСТ ФАЗЫ 2

После выполнения всех задач проверь:

- [ ] Заполнен `/data/landing/steps.ts` (7 шагов)
- [ ] Заполнен `/data/landing/benefits.ts` (6-8 преимуществ)
- [ ] Заполнен `/data/landing/faq.ts` (5-10 вопросов)
- [ ] Заполнен `/data/landing/tutorial.ts` (5 типов)
- [ ] Заполнен `/data/landing/mockData.ts` (проекты + templates)
- [ ] Все `TODO` комментарии удалены
- [ ] TypeScript компилируется без ошибок
- [ ] Тестовый импорт работает
- [ ] `app/page.tsx` НЕ изменен
- [ ] Dev сервер запускается и работает

---

## КОММИТ ПОСЛЕ ФАЗЫ 2

Когда все готово, создай коммит:

```bash
git add .
git commit -m "feat: Phase 2 - Extract data from landing page

- Filled data/landing/steps.ts (7 process steps)
- Filled data/landing/benefits.ts (X benefits)
- Filled data/landing/faq.ts (X FAQ items)
- Filled data/landing/tutorial.ts (5 tutorial types)
- Filled data/landing/mockData.ts (X projects, 2 templates)

- Removed all TODO comments
- All data properly typed according to types/landing.ts
- TypeScript compiles without errors

IMPORTANT: Original app/page.tsx is NOT changed
Phase 2/8 completed ✅

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## СЛЕДУЮЩИЕ ШАГИ

После успешного завершения Фазы 2:

1. Сообщить о готовности: "Фаза 2 завершена! Готов к Фазе 3"
2. **Фаза 3**: Извлечение утилит (реализация функций в `projectHelpers.ts`)

---

## ВРЕМЯ ВЫПОЛНЕНИЯ

**Оценка:** 1.5 часа

- Заполнение steps.ts: 20 минут
- Заполнение benefits.ts: 15 минут
- Заполнение faq.ts: 15 минут
- Заполнение tutorial.ts: 20 минут
- Заполнение mockData.ts: 20 минут
- Тестирование: 15 минут

---

## ВОПРОСЫ?

Если что-то непонятно:
1. Прочитай инструкции по конкретной задаче
2. Посмотри на примеры структуры данных
3. Проверь типы в `types/landing.ts`
4. Найди нужные данные в `app/page.tsx` через поиск по ключевым словам

Удачи! 🚀
