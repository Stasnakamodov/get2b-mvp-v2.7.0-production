# 🎯 ЗАДАЧА: Наполнение базы данных тестовыми данными

## 📋 Контекст проекта

Это B2B маркетплейс **Get2B** для работы с китайскими и международными поставщиками.

### Архитектура базы данных (Supabase PostgreSQL):

**Таблицы каталога:**
1. `catalog_categories` - категории товаров (3-уровневая иерархия)
2. `catalog_verified_suppliers` - аккредитованные поставщики (оранжевая комната)
3. `catalog_user_suppliers` - личные поставщики пользователя (синяя комната)
4. `catalog_products` - товары с привязкой к поставщикам

**Связи:**
- Товар → Поставщик (через supplier_id и supplier_type)
- Товар → Категория (через category поле - текстовое имя категории)
- Категория → Подкатегория (через parent_id и level)

### Текущее состояние:

**✅ Что УЖЕ работает:**
- 3-уровневая навигация по категориям (Категория → Подкатегория → Товары)
- В базе есть 41 категория (8 корневых + 33 подкатегории)
- localStorage для корзины с синхронизацией между страницами
- Кнопка корзины на главной странице dashboard

**❌ Что НЕ хватает:**
- Товаров в подкатегориях (нужно добавить минимум по 1 товару в каждую)
- Достаточного количества поставщиков для демонстрации

**⚠️ ВАЖНО: PostgREST cache проблема**
- После добавления данных через SQL нужно рестартнуть Supabase API
- Или использовать MCP Supabase для вставки данных

---

## 🎯 ТВОЯ ЗАДАЧА

### Цель:
Добавить тестовые данные в базу через **MCP Supabase** чтобы можно было протестировать весь флоу:
1. Открыть каталог
2. Выбрать категорию → увидеть подкатегории
3. Выбрать подкатегорию → увидеть товары
4. Добавить товары в корзину
5. Создать проект

### Шаг 1: Проверить структуру категорий

```sql
-- Получить все категории с их уровнями
SELECT id, name, level, parent_id, full_path, icon
FROM catalog_categories
ORDER BY level, parent_id, name;
```

**Ожидаемый результат:**
- 8 категорий уровня 0 (корневые): Электроника, Автотовары, Промышленность, и т.д.
- ~33 категории уровня 1 (подкатегории)

### Шаг 2: Проверить существующих поставщиков

```sql
-- Аккредитованные поставщики
SELECT id, name, company_name, category, country
FROM catalog_verified_suppliers
LIMIT 10;

-- Личные поставщики (если есть пользователь)
SELECT id, name, company_name, category, country
FROM catalog_user_suppliers
LIMIT 10;
```

### Шаг 3: Добавить поставщиков (если нужно)

Используй **MCP Supabase для INSERT**, например:

**Аккредитованные поставщики (минимум 5-10):**
- Категории: Электроника, Автотовары, Промышленность, Здоровье и медицина, Текстиль и одежда
- Страны: Китай, Турция, Индия, ОАЭ
- Поля: name, company_name, category, country, city, description, logo_url, contact_email, public_rating, reviews_count

**Личные поставщики (минимум 3-5):**
- Должны быть привязаны к user_id (возьми реального пользователя из auth.users)
- Те же поля + user_id

### Шаг 4: Добавить товары в КАЖДУЮ подкатегорию

**Важно:**
- Минимум 1-2 товара в каждую подкатегорию
- Поле `category` должно содержать ПОЛНОЕ имя подкатегории (например: "Смартфоны и планшеты")
- Поле `supplier_type` = 'verified' или 'user'
- Поле `supplier_id` = ID существующего поставщика

**Пример товара:**
```json
{
  "name": "iPhone 15 Pro Max",
  "description": "Смартфон Apple последнего поколения",
  "category": "Смартфоны и планшеты",
  "price": "1299.99",
  "currency": "USD",
  "min_order": 10,
  "in_stock": true,
  "supplier_id": "uuid-поставщика",
  "supplier_type": "verified",
  "images": ["https://example.com/iphone15.jpg"],
  "sku": "IPH15PM-256-BLK"
}
```

### Шаг 5: Проверить через UI

После добавления данных:

1. Запусти `npm run dev`
2. Открой `/dashboard/catalog`
3. Проверь:
   - ✅ Видны категории с правильными счётчиками товаров
   - ✅ При клике на категорию показываются подкатегории
   - ✅ При клике на подкатегорию показываются товары
   - ✅ Можно добавить товар в корзину
   - ✅ Корзина сохраняется в localStorage
   - ✅ На главной `/dashboard` появляется кнопка корзины со счётчиком

---

## 📊 Минимальные требования к данным

### Поставщики:
- **Аккредитованные**: минимум 8 (по 1 на каждую категорию)
- **Личные**: минимум 3

### Товары:
- **Всего**: минимум 33 товара (по 1 на подкатегорию)
- **Распределение по подкатегориям**:
  - Электроника: 4 подкатегории → 4 товара
  - Автотовары: 4 подкатегории → 4 товара
  - Промышленность: 4 подкатегории → 4 товара
  - Здоровье и медицина: 3 подкатегории → 3 товара
  - Текстиль и одежда: 4 подкатегории → 4 товара
  - Строительство: 4 подкатегории → 4 товара
  - Продукты питания: 4 подкатегории → 4 товара
  - Дом и быт: 4 подкатегории → 4 товара

### Критерии качества:
- ✅ Реалистичные названия и описания
- ✅ Правильные цены и валюты
- ✅ Логичная связь товар-поставщик-категория
- ✅ Разнообразие стран и категорий

---

## 🛠️ Инструменты для работы

### Используй MCP Supabase:
```bash
# Проверить доступные MCP инструменты
mcp__supabase__*
```

**Основные операции:**
1. `mcp__supabase__select` - SELECT запросы
2. `mcp__supabase__insert` - INSERT данных
3. `mcp__supabase__update` - UPDATE данных

### Пример использования:
```typescript
// Вставка поставщика
await mcp__supabase__insert({
  table: 'catalog_verified_suppliers',
  values: {
    name: 'Shenzhen Electronics Co',
    company_name: 'Shenzhen Electronics Manufacturing Ltd',
    category: 'Электроника',
    country: 'Китай',
    city: 'Шэньчжэнь',
    description: 'Производитель электроники с 15-летним опытом',
    public_rating: 4.8,
    reviews_count: 150
  }
})
```

---

## ⚠️ Важные замечания

1. **PostgREST cache**:
   - Если добавляешь через SQL, нужен рестарт API
   - Если через MCP Supabase - обновляется автоматически

2. **User ID для личных поставщиков**:
   - Сначала получи реального пользователя из auth.users
   - Используй его user_id для catalog_user_suppliers

3. **Категории - точное совпадение**:
   - Поле category в товаре должно ТОЧНО совпадать с name в catalog_categories
   - Регистр важен!

4. **Supplier type consistency**:
   - supplier_type = 'verified' → supplier_id из catalog_verified_suppliers
   - supplier_type = 'user' → supplier_id из catalog_user_suppliers

---

## 📝 Отчёт о выполнении

После завершения предоставь:

1. **Статистику добавленных данных:**
   - Количество поставщиков (verified/user)
   - Количество товаров по категориям
   - Список подкатегорий с товарами

2. **SQL запросы для проверки:**
   ```sql
   -- Товары по подкатегориям
   SELECT category, COUNT(*) as product_count
   FROM catalog_products
   GROUP BY category
   ORDER BY product_count DESC;

   -- Поставщики по категориям
   SELECT category, COUNT(*) as supplier_count
   FROM catalog_verified_suppliers
   GROUP BY category;
   ```

3. **Скриншоты/подтверждение:**
   - UI показывает категории с товарами
   - Можно добавить товар в корзину
   - Корзина работает

---

## 🎯 Критерии успеха

✅ В каждой подкатегории есть минимум 1 товар
✅ Есть минимум 8 аккредитованных поставщиков
✅ Есть минимум 3 личных поставщика
✅ 3-уровневая навигация работает полностью
✅ Корзина работает с localStorage
✅ Можно создать проект из корзины

---

## 📚 Полезные ссылки

- Supabase Dashboard: https://supabase.com/dashboard
- Структура категорий: `/docs/CATEGORY_TREE_FULL.md`
- Миграции БД: `/supabase/migrations/`

---

**Время выполнения**: ~30-60 минут
**Приоритет**: HIGH
**Блокирует**: Тестирование полного флоу работы с каталогом

Удачи! 🚀
