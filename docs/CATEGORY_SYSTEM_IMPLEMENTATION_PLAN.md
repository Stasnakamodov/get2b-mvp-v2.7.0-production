# 📋 ПЛАН ВНЕДРЕНИЯ СИСТЕМЫ КАТЕГОРИЙ И ПОДКАТЕГОРИЙ В GET2B

**Дата создания:** 2025-10-30
**Проект:** Get2B - B2B маркетплейс
**Цель:** Добавить иерархическую систему категорий и подкатегорий в каталог поставщиков

---

## 📊 EXECUTIVE SUMMARY

### Текущая ситуация
- ✅ Приложение: Next.js 15.2.4 + Supabase + TypeScript
- ✅ Статические категории: 8 основных категорий в коде (`lib/constants/categories.ts`)
- ✅ Таблица БД: `supplier_profiles` с колонкой `category` (text, nullable)
- ⚠️ **КРИТИЧНО:** Таблицы `supplier_profiles` и `client_profiles` **ПУСТЫЕ (0 записей)**
- ✅ Нет внешних ключей, нет зависимостей
- ✅ MCP Context7 подключен и настроен

### Что добавляем
- 🆕 Таблица `catalog_categories` с поддержкой иерархии (3 уровня)
- 🆕 8 основных B2B FMCG категорий
- 🆕 ~25 подкатегорий (уровень 2)
- 🆕 ~70 детальных категорий (уровень 3)
- 🆕 Связи `supplier_profiles.category_id` → `catalog_categories.id`

### Оценка рисков
- **РИСК СЛОМАТЬ ДАННЫЕ:** 0% (таблицы пустые)
- **РИСК СЛОМАТЬ КОД:** 5% (обратная совместимость сохранена)
- **ВОЗМОЖНОСТЬ ОТКАТА:** 100% (все миграции обратимы)

---

## 🎯 ЦЕЛЕВАЯ СТРУКТУРА КАТЕГОРИЙ

### Основные категории (8 шт, уровень 0)

```
1. 🍽️ Продукты питания
2. 🏠 Товары для дома и HoReCa
3. 💄 Косметика и гигиена
4. 👕 Одежда и текстиль
5. 🔌 Электроника и гаджеты
6. 🏢 Офис и канцелярия
7. 🚗 Автотовары
8. 📦 Упаковка и расходники
```

### Пример иерархии (3 уровня)

```
🍽️ Продукты питания (level 0)
├── Напитки (level 1)
│   ├── Вода питьевая (level 2)
│   ├── Соки и нектары (level 2)
│   └── Чай и кофе (level 2)
├── Снеки и сладости (level 1)
│   ├── Шоколад и конфеты (level 2)
│   ├── Печенье и вафли (level 2)
│   └── Чипсы и сухарики (level 2)
└── Бакалея (level 1)
    ├── Макароны и крупы (level 2)
    ├── Масло растительное (level 2)
    └── Консервы (level 2)
```

**Полную структуру смотрите в:** `docs/CATEGORY_TREE_FULL.md`

---

## 🗄️ СХЕМА БАЗЫ ДАННЫХ

### Новая таблица: `catalog_categories`

```sql
CREATE TABLE catalog_categories (
  -- Идентификаторы
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  parent_id uuid REFERENCES catalog_categories(id) ON DELETE CASCADE,

  -- Основная информация
  name text NOT NULL,
  slug text UNIQUE NOT NULL,
  icon text DEFAULT '📦',
  description text,

  -- Иерархия
  level integer DEFAULT 0 CHECK (level BETWEEN 0 AND 3),
  sort_order integer DEFAULT 0,
  full_path text, -- "Продукты питания / Напитки / Вода питьевая"

  -- Метрики (автоматически обновляются)
  products_count integer DEFAULT 0,
  suppliers_count integer DEFAULT 0,

  -- Статус
  is_active boolean DEFAULT true,
  is_popular boolean DEFAULT false,

  -- Дополнительные данные
  metadata jsonb DEFAULT '{}',

  -- Временные метки
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);

-- Индексы для производительности
CREATE INDEX idx_cat_parent ON catalog_categories(parent_id) WHERE parent_id IS NOT NULL;
CREATE INDEX idx_cat_slug ON catalog_categories(slug);
CREATE INDEX idx_cat_level ON catalog_categories(level);
CREATE INDEX idx_cat_active ON catalog_categories(is_active) WHERE is_active = true;
CREATE INDEX idx_cat_popular ON catalog_categories(is_popular) WHERE is_popular = true;
```

### Изменения в `supplier_profiles`

```sql
ALTER TABLE supplier_profiles
  ADD COLUMN category_id uuid REFERENCES catalog_categories(id) ON DELETE SET NULL,
  ADD COLUMN subcategory_id uuid REFERENCES catalog_categories(id) ON DELETE SET NULL;

CREATE INDEX idx_supplier_category ON supplier_profiles(category_id);
CREATE INDEX idx_supplier_subcategory ON supplier_profiles(subcategory_id);

-- ВАЖНО: Старая колонка category (text) ОСТАЕТСЯ для обратной совместимости!
```

---

## 📁 СТРУКТУРА ФАЙЛОВ

### Миграции
```
supabase/migrations/
├── 20251030_001_create_catalog_categories.sql
├── 20251030_002_add_category_links_to_suppliers.sql
└── 20251030_003_seed_categories.sql
```

### Seed данные
```
lib/seed-data/
└── categories-fmcg-b2b.json
```

### Типы TypeScript
```
lib/types/
├── category.types.ts
└── supplier.types.ts (обновлён)
```

### API Routes
```
app/api/catalog/
├── categories/
│   ├── route.ts (обновлён)
│   └── [slug]/
│       ├── route.ts (новый)
│       └── subcategories/route.ts (новый)
└── category-tree/route.ts (новый)
```

### Компоненты
```
components/catalog/
├── CategorySelector.tsx (старый - остаётся)
├── CategoryTreeSelector.tsx (новый)
├── CategoryBreadcrumbs.tsx (новый)
└── CategoryFilters.tsx (обновлён)
```

---

## 🔄 ПЛАН ВНЕДРЕНИЯ (ПОЭТАПНО)

### ЭТАП 1: Подготовка и аудит ✅ ЗАВЕРШЁН

**Что сделано:**
- ✅ Изучена текущая структура приложения
- ✅ Проверено подключение MCP Context7
- ✅ Проведён аудит БД (0 записей в таблицах)
- ✅ Проверены внешние ключи (отсутствуют)
- ✅ Разработана структура категорий (8/25/70)
- ✅ Оценены риски (0% потери данных)

**Результат:** План безопасен для выполнения

---

### ЭТАП 2: Создание структуры БД (1-2 часа)

**Файлы:**
1. `supabase/migrations/20251030_001_create_catalog_categories.sql`
2. `supabase/migrations/20251030_002_add_category_links_to_suppliers.sql`

**Действия:**
```bash
# 1. Создать миграции локально
# 2. Проверить SQL синтаксис
# 3. Применить через Supabase CLI или Dashboard
npx supabase migration up

# 4. Проверить создание таблиц
npx supabase db diff
```

**Откат (если нужно):**
```sql
DROP TABLE IF EXISTS catalog_categories CASCADE;
ALTER TABLE supplier_profiles
  DROP COLUMN IF EXISTS category_id,
  DROP COLUMN IF EXISTS subcategory_id;
```

**Критерии успеха:**
- ✅ Таблица `catalog_categories` создана
- ✅ Индексы созданы
- ✅ Триггер `updated_at` работает
- ✅ Связи с `supplier_profiles` добавлены

**Риск:** 0% (новая таблица, обратимо)

---

### ЭТАП 3: Создание seed данных (2-3 часа)

**Файлы:**
1. `lib/seed-data/categories-fmcg-b2b.json`
2. `scripts/import-categories.ts`
3. `supabase/migrations/20251030_003_seed_categories.sql`

**Структура JSON:**
```json
{
  "version": "1.0",
  "categories": [
    {
      "name": "Продукты питания",
      "slug": "produkty-pitaniya",
      "icon": "🍽️",
      "level": 0,
      "sort_order": 1,
      "description": "Продукты питания для B2B: напитки, снеки, бакалея",
      "subcategories": [
        {
          "name": "Напитки",
          "slug": "napitki",
          "icon": "🥤",
          "level": 1,
          "sort_order": 1,
          "items": [
            {
              "name": "Вода питьевая",
              "slug": "voda-pitevaya",
              "level": 2,
              "sort_order": 1
            }
          ]
        }
      ]
    }
  ]
}
```

**Действия:**
```bash
# 1. Создать JSON с полной структурой
# 2. Создать TypeScript скрипт импорта
npx ts-node scripts/import-categories.ts

# 3. Проверить данные в БД
npx supabase db query "SELECT COUNT(*) FROM catalog_categories"
```

**Откат:**
```sql
DELETE FROM catalog_categories;
ALTER SEQUENCE catalog_categories_id_seq RESTART WITH 1;
```

**Критерии успеха:**
- ✅ 8 основных категорий добавлены
- ✅ ~25 подкатегорий добавлены
- ✅ ~70 детальных категорий добавлены
- ✅ Иерархия parent_id корректна
- ✅ Поле full_path заполнено

**Риск:** 0% (только вставка данных, обратимо)

---

### ЭТАП 4: TypeScript типы (30 минут)

**Файлы:**
1. `lib/types/category.types.ts` (новый)
2. `lib/types/supplier.types.ts` (обновить)

**Содержимое `category.types.ts`:**
```typescript
export interface CatalogCategory {
  id: string
  parent_id: string | null
  name: string
  slug: string
  icon: string
  description: string | null
  level: 0 | 1 | 2 | 3
  sort_order: number
  full_path: string | null
  products_count: number
  suppliers_count: number
  is_active: boolean
  is_popular: boolean
  metadata: Record<string, any>
  created_at: string
  updated_at: string

  // Виртуальные поля (не в БД)
  subcategories?: CatalogCategory[]
  parent?: CatalogCategory
}

export interface CategoryTree {
  category: CatalogCategory
  children: CategoryTree[]
  depth: number
  path: string[]
}

export interface CategoryBreadcrumb {
  id: string
  name: string
  slug: string
  level: number
}
```

**Критерии успеха:**
- ✅ Типы совпадают со схемой БД
- ✅ Нет ошибок TypeScript
- ✅ Экспортированы все нужные типы

**Риск:** 0% (только типы, ничего не ломается)

---

### ЭТАП 5: Backend API (3-4 часа)

**Обновляемые endpoints:**

#### 5.1 `GET /api/catalog/categories` (обновить)
```typescript
// Текущий: возвращает плоский список
// Новый: возвращает дерево + плоский список

Response:
{
  success: true,
  categories: CatalogCategory[], // плоский
  categoryTree: CategoryTree[],  // дерево
  stats: {
    total: number,
    byLevel: { 0: number, 1: number, 2: number }
  }
}
```

#### 5.2 `GET /api/catalog/category-tree` (новый)
```typescript
// Полное дерево всех категорий с вложенностью
Response: CategoryTree[]
```

#### 5.3 `GET /api/catalog/categories/[slug]` (новый)
```typescript
// Информация о конкретной категории + подкатегории
Response: {
  category: CatalogCategory,
  subcategories: CatalogCategory[],
  breadcrumbs: CategoryBreadcrumb[],
  relatedCategories: CatalogCategory[]
}
```

#### 5.4 `GET /api/catalog/categories/[slug]/products` (новый)
```typescript
// Товары/поставщики в категории
Query params: ?include_subcategories=true&page=1&limit=20

Response: {
  category: CatalogCategory,
  suppliers: Supplier[],
  products: Product[],
  pagination: {...}
}
```

**Действия:**
```bash
# 1. Обновить существующие API
# 2. Создать новые endpoints
# 3. Добавить обработку ошибок
# 4. Тестировать через Postman/curl
```

**Критерии успеха:**
- ✅ Все endpoints возвращают корректные данные
- ✅ Обратная совместимость сохранена
- ✅ Обработка ошибок работает
- ✅ Производительность приемлемая (<200ms)

**Риск:** 5% (может сломать старый код если API изменён неправильно)

**Откат:** Git revert + redeploy

---

### ЭТАП 6: Frontend компоненты (4-6 часов)

**Новые компоненты:**

#### 6.1 `CategoryTreeSelector.tsx`
```typescript
interface Props {
  selectedCategoryId?: string
  onSelectCategory: (category: CatalogCategory) => void
  maxDepth?: number // 0, 1, 2, 3
  showCounts?: boolean
}

// Отображает дерево категорий с раскрывающимися уровнями
```

#### 6.2 `CategoryBreadcrumbs.tsx`
```typescript
interface Props {
  categoryId: string
  onCategoryClick: (categoryId: string) => void
}

// Главная > Продукты питания > Напитки > Вода
```

#### 6.3 `CategoryFilters.tsx` (обновить)
```typescript
// Добавить фильтрацию по подкатегориям
// Множественный выбор категорий
```

**Обновлённые компоненты:**

#### 6.4 `CatalogPageRefactored.tsx`
```typescript
// Интегрировать CategoryTreeSelector
// Добавить CategoryBreadcrumbs
// Обновить фильтрацию
```

**Действия:**
```bash
# 1. Создать компоненты
# 2. Добавить стили (Tailwind)
# 3. Интегрировать с API
# 4. Тестировать в браузере
npm run dev
```

**Критерии успеха:**
- ✅ Компоненты рендерятся без ошибок
- ✅ Навигация по категориям работает
- ✅ Breadcrumbs отображаются корректно
- ✅ Фильтрация работает
- ✅ UI/UX соответствует дизайну

**Риск:** 3% (CSS конфликты, ошибки в логике)

**Откат:** Git revert

---

### ЭТАП 7: Интеграция и тестирование (2-3 часа)

**Тесты:**

1. **Unit тесты** (если есть)
```bash
npm run test
```

2. **E2E тесты** (если есть)
```bash
npm run test:e2e
```

3. **Ручное тестирование:**
- ✅ Выбор категории уровня 0 → показывает подкатегории
- ✅ Выбор категории уровня 1 → показывает детальные
- ✅ Breadcrumbs корректны
- ✅ Фильтры работают
- ✅ Поиск работает с категориями
- ✅ Mobile responsive

**Критерии успеха:**
- ✅ Все тесты проходят
- ✅ Нет ошибок в консоли
- ✅ Производительность приемлемая
- ✅ UX интуитивный

**Риск:** 2% (неожиданные edge cases)

---

### ЭТАП 8: Feature Flag и постепенный rollout (1 час)

**Реализация Feature Flag:**

```typescript
// lib/config/features.ts
export const FEATURES = {
  USE_CATEGORY_TREE: process.env.NEXT_PUBLIC_FEATURE_CATEGORY_TREE === 'true'
}

// В компонентах:
import { FEATURES } from '@/lib/config/features'

{FEATURES.USE_CATEGORY_TREE ? (
  <CategoryTreeSelector />
) : (
  <CategorySelector /> // старый
)}
```

**.env файлы:**
```bash
# .env.local (для локальной разработки)
NEXT_PUBLIC_FEATURE_CATEGORY_TREE=true

# .env.production (для production)
NEXT_PUBLIC_FEATURE_CATEGORY_TREE=false # сначала выключено

# После тестирования:
NEXT_PUBLIC_FEATURE_CATEGORY_TREE=true # включаем для всех
```

**План rollout:**
1. День 1: Включаем только для разработчиков (локально)
2. День 2-3: Staging environment (если есть)
3. День 4: Production с флагом OFF
4. День 5: Включаем для админов
5. День 6-7: Мониторим ошибки
6. День 8: Включаем для всех пользователей

**Критерии успеха:**
- ✅ Feature flag работает
- ✅ Можно включать/выключать без redeploy
- ✅ Старый функционал доступен

**Риск:** 1% (ошибка в условной логике)

---

## 📊 МОНИТОРИНГ И МЕТРИКИ

### Что отслеживать после внедрения:

1. **Производительность:**
   - Время загрузки дерева категорий
   - Время фильтрации по категориям
   - Database query time

2. **Ошибки:**
   - JavaScript errors в консоли
   - API errors (4xx, 5xx)
   - Database errors

3. **Использование:**
   - Какие категории популярны
   - Глубина навигации (level 0/1/2)
   - Conversion rate

**Инструменты:**
- Supabase Dashboard (database logs)
- Vercel Analytics (если на Vercel)
- Google Analytics / Posthog
- Sentry (error tracking)

---

## 🔙 ПЛАН ОТКАТА (ROLLBACK)

### Сценарий 1: Критическая ошибка в production

```bash
# 1. Выключить feature flag
NEXT_PUBLIC_FEATURE_CATEGORY_TREE=false

# 2. Redeploy
npm run build
npm run deploy

# 3. Удалить новые компоненты из сборки (если нужно)
git revert <commit-hash>
```

### Сценарий 2: Проблемы с БД

```sql
-- 1. Удалить новые колонки из supplier_profiles
ALTER TABLE supplier_profiles
  DROP COLUMN IF EXISTS category_id,
  DROP COLUMN IF EXISTS subcategory_id;

-- 2. Удалить таблицу категорий
DROP TABLE IF EXISTS catalog_categories CASCADE;

-- 3. Очистить миграции
DELETE FROM supabase_migrations
WHERE name IN (
  '20251030_001_create_catalog_categories',
  '20251030_002_add_category_links_to_suppliers',
  '20251030_003_seed_categories'
);
```

### Сценарий 3: Частичный откат (только frontend)

```bash
git revert <frontend-commit>
npm run build
npm run deploy
# БД остаётся, но UI возвращается к старому
```

**Время отката:** 5-10 минут

---

## ✅ ЧЕКЛИСТ ПЕРЕД ВНЕДРЕНИЕМ

### Подготовка
- [ ] Создана ветка `feature/category-hierarchy`
- [ ] Все миграции написаны и проверены
- [ ] Seed данные подготовлены
- [ ] TypeScript типы созданы
- [ ] Документация актуальна

### Разработка
- [ ] Миграции БД применены локально
- [ ] Seed данные загружены
- [ ] API endpoints работают
- [ ] Frontend компоненты работают
- [ ] Feature flag настроен

### Тестирование
- [ ] Unit тесты (если есть) проходят
- [ ] Ручное тестирование выполнено
- [ ] Mobile responsive проверен
- [ ] Производительность приемлемая
- [ ] Нет ошибок в консоли

### Деплой
- [ ] Code review выполнен
- [ ] Мердж в main
- [ ] CI/CD pipeline проходит
- [ ] Деплой на staging (если есть)
- [ ] Миграции применены на production
- [ ] Feature flag изначально OFF

### Post-deploy
- [ ] Мониторинг настроен
- [ ] Ошибок нет
- [ ] Feature flag включён
- [ ] Метрики собираются
- [ ] Пользователи довольны

---

## 📞 КОНТАКТЫ И ПОДДЕРЖКА

**Разработчик:** Claude Code
**Документация:** `/docs/CATEGORY_SYSTEM_IMPLEMENTATION_PLAN.md`
**Структура категорий:** `/docs/CATEGORY_TREE_FULL.md`
**Seed данные:** `/lib/seed-data/categories-fmcg-b2b.json`

---

## 🎯 КРИТЕРИИ УСПЕХА ПРОЕКТА

1. ✅ Система категорий работает без ошибок
2. ✅ Производительность не ухудшилась
3. ✅ UX улучшился (навигация проще)
4. ✅ Нет потерянных данных
5. ✅ Обратная совместимость сохранена
6. ✅ Можно откатить в любой момент
7. ✅ Пользователи могут легко находить поставщиков
8. ✅ Админы могут управлять категориями

---

**СТАТУС:** Готов к выполнению
**СЛЕДУЮЩИЙ ШАГ:** Ревью этого плана через новый чат с MCP Context7

---

## 📝 ПРИМЕЧАНИЯ

- Все изменения обратимы
- Данных для миграции нет (таблицы пустые)
- Риск минимален
- Можно выполнять поэтапно
- Feature flag обеспечивает безопасность
- Старый функционал остаётся работать

**Время выполнения:** 2-3 дня (при полной занятости)
**Риск:** НИЗКИЙ (0-5%)
**ROI:** ВЫСОКИЙ (улучшенная навигация, лучший UX)
