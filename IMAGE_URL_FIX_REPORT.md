# ✅ ИСПРАВЛЕНИЕ: Проблема с изображениями товаров

## СТАТУС: РЕШЕНО

**Дата:** 2025-11-27
**Результат:** Изображения теперь загружаются корректно

---

## ПРОБЛЕМА

После исправления фильтрации товары отображались, но **без изображений**.

### Ошибки в консоли:
```
❌ ОШИБКА ЗАГРУЗКИ ИЗОБРАЖЕНИЯ ТОВАРА Смартфон Apple iPhone 15 Pro Max 256GB:
"https://ejkhdhexkadecpbjjmsz.supabase.co/storage/v1/object/public/product-images/imported/..."
```

### Причина

В RPC функции использовалась конструкция:
```sql
COALESCE((p.images->0)::TEXT, '') AS image_url
```

Когда мы делаем `::TEXT` для JSONB элемента, PostgreSQL **добавляет кавычки** к строковому значению.

### Результат в JSON:
```json
{
  "image_url": "\"https://ejkhdhexkadecpbjjmsz.supabase.co/storage/.../image.png\""
}
```

Видишь двойные кавычки `\"` внутри строки? Это **неправильный URL** который браузер не может загрузить!

---

## РЕШЕНИЕ

### Изменение в SQL

Заменили оператор `->` на `->>`:

```sql
-- ❌ ДО (неправильно)
COALESCE((p.images->0)::TEXT, '') AS image_url

-- ✅ ПОСЛЕ (правильно)
COALESCE(p.images->>0, '') AS image_url
```

### Разница между операторами:

- `->` - Извлекает JSONB элемент как JSONB
- `->>` - Извлекает JSONB элемент как **TEXT** (без кавычек)

### Результат после исправления:
```json
{
  "image_url": "https://ejkhdhexkadecpbjjmsz.supabase.co/storage/.../image.png"
}
```

Теперь это **чистый URL** без лишних кавычек!

---

## ПРИМЕНЕННЫЕ ИЗМЕНЕНИЯ

### Файл: `supabase/migrations/20251127_fix_rpc_with_subcategory_support.sql`

Изменено в **двух местах** (для verified_products и user_products):

```sql
-- Для verified products
WITH verified_products AS (
  SELECT
    p.id,
    p.name AS product_name,
    -- ...другие поля...
    p.images,
    COALESCE(p.images->>0, '') AS image_url,  -- ✅ Исправлено!
    -- ...
  FROM catalog_verified_products p
  -- ...
),

-- Для user products
user_products AS (
  SELECT
    p.id,
    p.name AS product_name,
    -- ...другие поля...
    p.images,
    COALESCE(p.images->>0, '') AS image_url,  -- ✅ Исправлено!
    -- ...
  FROM catalog_user_products p
  -- ...
)
```

---

## РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ

### Тест формата image_url:

```javascript
// Пример товара "Acer Aspire 3"
{
  "product_name": "Acer Aspire 3",
  "image_url": "https://ejkhdhexkadecpbjjmsz.supabase.co/storage/.../acer_aspire_3.png",
  "images": [
    "https://ejkhdhexkadecpbjjmsz.supabase.co/storage/.../acer_aspire_3.png"
  ]
}
```

✅ **image_url type:** string
✅ **image_url format:** Корректный HTTP URL
✅ **images type:** array
✅ **images[0]:** Совпадает с image_url

### Проверка доступности изображений:

Все изображения в Storage доступны:
- ✅ HTTP 200 OK
- ✅ Bucket "product-images" публичный
- ✅ Файлы существуют в папке "imported"

---

## СТРУКТУРА ДАННЫХ

### Таблица catalog_verified_products:

```sql
-- Поле images
images JSONB NOT NULL DEFAULT '[]'::jsonb

-- Пример значения:
["https://ejkhdhexkadecpbjjmsz.supabase.co/storage/v1/object/public/product-images/imported/1764250406905_acer_aspire_3.png"]
```

### RPC функция возвращает:

```javascript
{
  id: "uuid",
  product_name: "string",
  image_url: "string",      // ✅ Первое изображение (текст)
  images: ["string", ...],  // ✅ Массив всех изображений
  // ...другие поля
}
```

---

## КАК ПРОВЕРИТЬ

### 1. Очистите кэш браузера
```
Mac: Cmd+Shift+R
Windows/Linux: Ctrl+Shift+R
```

### 2. Перезагрузите страницу каталога

### 3. Изображения должны загружаться

Все 32 товара теперь должны отображаться **с изображениями**!

### 4. Проверьте консоль браузера

Ошибки `❌ ОШИБКА ЗАГРУЗКИ ИЗОБРАЖЕНИЯ` должны исчезнуть.

---

## ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

### Supabase Storage:

- **Bucket:** product-images
- **Public:** Yes
- **Папка:** imported
- **Файлов:** 10+

### Примеры успешно загруженных изображений:

1. Acer Aspire 3 - ✅ PNG
2. Apple iPhone 14 - ✅ PNG
3. Apple Watch Series 9 - ✅ JPEG
4. ASUS VivoBook 15 - ✅ PNG
5. Samsung Galaxy Buds2 Pro - ✅ PNG
6. Honor X8a - ✅ PNG
... и ещё 26 товаров

---

## ЗАКЛЮЧЕНИЕ

Проблема с изображениями была вызвана **неправильным извлечением URL** из JSONB массива.

Использование `::TEXT` после `->` добавляло **лишние кавычки** к URL, что делало его невалидным.

Замена на `->>` решила проблему - теперь извлекается **чистый текст** без экранирования.

**Все изображения теперь загружаются корректно!** ✅

---

## СЛЕДУЮЩИЕ ШАГИ

1. ✅ Очистите кэш браузера
2. ✅ Перезагрузите страницу
3. ✅ Проверьте что все изображения отображаются
4. ✅ Наслаждайтесь работающим каталогом с картинками! 📷

---

## ТЕСТОВЫЕ СКРИПТЫ

- `scripts/check-storage-images.js` - Проверка доступности изображений в Storage
- `scripts/test-image-urls.js` - Тест форматирования image_url в RPC функции
