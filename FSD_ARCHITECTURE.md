# Feature-Sliced Design Architecture

## 📁 Структура проекта

```
src/
├── app/                 # Инициализация приложения
├── pages/              # Страницы (Next.js pages или app directory)
├── widgets/            # Композиция фич в крупные блоки
│   └── catalog-suppliers/
│       ├── ui/
│       └── index.ts
├── features/           # Пользовательские сценарии
│   └── manage-suppliers/
│       ├── model/
│       ├── ui/
│       └── index.ts
├── entities/           # Бизнес-сущности
│   ├── supplier/
│   │   ├── model/
│   │   ├── api/
│   │   ├── ui/
│   │   └── index.ts
│   └── product/
├── shared/            # Переиспользуемые компоненты
│   ├── api/
│   ├── config/
│   ├── lib/
│   └── ui/
└── app/dashboard/catalog/  # Старая структура (постепенная миграция)
    ├── hooks/
    ├── services/
    ├── constants/
    └── utils/
```

## 🎯 Прогресс миграции

### ✅ Завершено (Phase 1 - Option A)
- [x] Создан hook useSuppliers
- [x] Создан hook useCategories
- [x] Создан supplierService
- [x] Создан productService
- [x] Интегрированы хуки в page.tsx

### 🚀 В процессе (Phase 2 - Option B)
- [x] Создана FSD структура папок
- [x] **Shared layer**:
  - config/index.ts - конфигурация
  - ui/Button - базовые UI компоненты
- [x] **Entities layer**:
  - supplier - типы, API, карточка поставщика
- [x] **Features layer**:
  - manage-suppliers - управление поставщиками
- [x] **Widgets layer**:
  - catalog-suppliers - виджет каталога
- [ ] **Pages layer** - рефакторинг страниц
- [ ] **App layer** - провайдеры и инициализация

### 📋 План дальнейшей миграции

#### Следующие шаги:
1. Создать entities/product
2. Создать features/manage-products
3. Создать widgets/catalog-products
4. Мигрировать компоненты корзины
5. Рефакторинг главной страницы каталога

## 🔄 Стратегия миграции

### Инкрементальный подход:
1. **Не ломаем работающий код** - старая структура работает параллельно
2. **Постепенная миграция** - переносим по одному модулю
3. **Тестирование на каждом шаге** - проверяем работоспособность
4. **Документирование** - обновляем этот файл

### Правила FSD:
- **Однонаправленный поток зависимостей** - слои импортируют только нижележащие
- **Public API через index.ts** - каждый модуль экспортирует только публичное API
- **Изоляция слайсов** - слайсы на одном уровне не знают друг о друге
- **Композиция в widgets** - виджеты объединяют несколько фич

## 🛠 Использование новой архитектуры

### Пример импорта:
```typescript
// ✅ Правильно - импорт через public API
import { SupplierCard } from '@/src/entities/supplier'
import { useManageSuppliers } from '@/src/features/manage-suppliers'

// ❌ Неправильно - прямой импорт внутренних модулей
import { SupplierCard } from '@/src/entities/supplier/ui/SupplierCard'
```

### Пример композиции:
```typescript
// Widget объединяет несколько features
export function CatalogWidget() {
  const suppliers = useManageSuppliers()
  const products = useManageProducts()

  return (
    <>
      <SupplierList suppliers={suppliers} />
      <ProductGrid products={products} />
    </>
  )
}
```

## 📈 Преимущества FSD

1. **Масштабируемость** - легко добавлять новые фичи
2. **Поддерживаемость** - понятная структура и зависимости
3. **Переиспользование** - shared компоненты для всего приложения
4. **Изоляция** - изменения в одном модуле не ломают другие
5. **Тестирование** - каждый слой тестируется отдельно

## 🔗 Ссылки

- [Feature-Sliced Design](https://feature-sliced.design/)
- [FSD в Next.js](https://feature-sliced.design/docs/guides/tech/with-nextjs)
- [Примеры проектов](https://feature-sliced.design/examples)