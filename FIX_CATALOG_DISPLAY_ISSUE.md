# 🔧 РЕШЕНИЕ ПРОБЛЕМЫ ОТОБРАЖЕНИЯ ТОВАРОВ В КАТАЛОГЕ

## 🔴 Проблема
Категория "ТЕСТОВАЯ" показывает "0 товаров найдено", хотя в базе данных есть 111 товаров.

## ✅ Причина найдена!
В браузере сохранен старый поставщик в localStorage (`catalog_active_supplier`), который фильтрует все товары. Компонент ProductGridByCategory фильтрует товары по activeSupplier, если он установлен.

## 📍 Где проблема в коде:

1. **app/dashboard/catalog/page.tsx:602** - activeSupplier загружается из localStorage
2. **components/catalog/ProductGridByCategory.tsx:240-243** - фильтрация по activeSupplier
3. Если activeSupplier не соответствует поставщикам товаров, показывается 0 товаров

## 🛠️ РЕШЕНИЕ

### Вариант 1: Очистить localStorage в браузере (быстрое решение)

Откройте консоль браузера (F12) и выполните:
```javascript
localStorage.removeItem('catalog_active_supplier');
localStorage.removeItem('catalog_cart');
location.reload();
```

### Вариант 2: Добавить кнопку сброса фильтра

В файле `app/dashboard/catalog/page.tsx` после строки 582 добавьте функцию:

```typescript
const resetSupplierFilter = () => {
  setActiveSupplier(null);
  localStorage.removeItem('catalog_active_supplier');
  localStorage.removeItem('catalog_cart');
  setCart([]);
};
```

И добавьте кнопку в UI для сброса фильтра.

### Вариант 3: Исправить логику фильтрации (постоянное решение)

В файле `components/catalog/ProductGridByCategory.tsx` строка 240-244:

**БЫЛО:**
```typescript
// НОВОЕ: Фильтрация по активному поставщику
if (activeSupplier) {
  filtered = filtered.filter(product =>
    product.supplier_name === activeSupplier || product.supplier_company_name === activeSupplier
  )
}
```

**ИСПРАВЛЕНИЕ:**
```typescript
// НОВОЕ: Фильтрация по активному поставщику
// Применяем фильтр только если есть товары в корзине
if (activeSupplier && cart && cart.length > 0) {
  filtered = filtered.filter(product =>
    product.supplier_name === activeSupplier || product.supplier_company_name === activeSupplier
  )
}
```

## 📋 Проверка решения:

1. Откройте http://localhost:3000/catalog
2. Нажмите на категорию "ТЕСТОВАЯ"
3. Должны отобразиться 111 товаров

## ✅ Что работает правильно:
- ✅ В БД есть 111 товаров
- ✅ RPC функция возвращает товары
- ✅ API endpoint возвращает товары
- ✅ Frontend получает товары

## ❌ Что не работает:
- ❌ Frontend фильтрует товары по старому поставщику из localStorage

---

## 🚀 Быстрое решение для пользователя:

1. Откройте страницу каталога: http://localhost:3000/catalog
2. Откройте консоль браузера (F12)
3. Вставьте и выполните этот код:
```javascript
localStorage.clear();
location.reload();
```
4. Товары должны появиться!

---

*После очистки localStorage товары будут отображаться корректно.*