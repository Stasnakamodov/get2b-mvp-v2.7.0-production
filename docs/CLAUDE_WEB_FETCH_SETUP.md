# Claude Web Fetch - Настройка и тестирование

## 🎯 Что это?

Claude Web Fetch - это встроенный инструмент Claude API, который позволяет AI:
- Загружать HTML контент по URL
- Анализировать страницы товаров
- Извлекать метаданные (название, бренд, категория, цена)
- Генерировать ключевые слова для поиска

**Преимущества:**
- ✅ Не нужны дополнительные библиотеки для парсинга
- ✅ AI понимает контекст и извлекает нужную информацию
- ✅ Дешево: ~0.27₽ за запрос
- ✅ Быстро: 2-5 секунд

**Ограничения:**
- ❌ НЕ обходит Cloudflare/anti-bot защиту
- ❌ Для Ozon/Wildberries требуется fallback на ScraperAPI или Playwright

---

## 📝 Шаг 1: Получение API ключа

1. **Зайди на:** https://console.anthropic.com/
2. **Создай аккаунт** (бесплатно)
3. **Получи $5 бесплатных кредитов** (~18,500 запросов Haiku)
4. **Создай API ключ:**
   - Settings → API Keys
   - Create Key
   - Скопируй ключ (начинается с `sk-ant-`)

---

## ⚙️ Шаг 2: Настройка проекта

### 2.1. Добавь API ключ в `.env.local`:

```bash
# Claude API (для Web Fetch парсинга)
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxx
```

### 2.2. Убедись что пакет установлен:

```bash
npm list @anthropic-ai/sdk
```

Если не установлен:
```bash
npm install @anthropic-ai/sdk --legacy-peer-deps
```

---

## 🧪 Шаг 3: Тестирование

### Тест 1: Проверка Claude Web Fetch на разных сайтах

```bash
ANTHROPIC_API_KEY=sk-ant-xxx node scripts/test-claude-web-fetch.js
```

**Что тестируем:**
- ✅ Ozon (ожидаем блокировку Cloudflare)
- ✅ Wildberries (ожидаем блокировку)
- ✅ Яндекс.Маркет (может сработать)
- ✅ Habr (простой сайт, должен работать)

**Ожидаемый результат:**
```
=======================================================================
📦 ТЕСТ: Ozon (защищен Cloudflare)
🔗 URL: https://www.ozon.ru/product/...
⚡ Сложность: HARD
=======================================================================

⏳ Отправляем запрос в Claude...
✅ Ответ получен за 3.45с

📄 Ответ Claude:
{
  "brand": null,
  "category": null,
  "keywords": [],
  "description": "Доступ к странице заблокирован Cloudflare"
}

⚠️ СТАТУС: ЗАБЛОКИРОВАН (нужен ScraperAPI)
💰 Использовано токенов: Input: 487, Output: 156
  Стоимость: $0.0009 (~0.08₽)
```

### Тест 2: Проверка через API endpoint

```bash
curl -X POST http://localhost:3000/api/catalog/search-by-url \
  -H "Content-Type: application/json" \
  -d '{"url": "https://habr.com/ru/articles/"}'
```

---

## 📊 Что делает система?

### Стратегия парсинга (приоритет):

```
1️⃣ CLAUDE WEB FETCH (если доступен API ключ)
   ↓ Быстро (2-5 сек), дешево (0.27₽)
   ↓ Работает для ~30% сайтов
   ↓
   ✅ Успех → Возвращаем результат
   ❌ Блокировка → Переходим к шагу 2

2️⃣ PLAYWRIGHT + STEALTH (fallback)
   ↓ Медленно (10-15 сек), бесплатно
   ↓ Работает для ~20% защищенных сайтов
   ↓
   ✅ Успех → Возвращаем результат
   ❌ Блокировка → Показываем ошибку

3️⃣ РУЧНОЙ ВВОД HTML (последний fallback)
   ↓ Пользователь копирует HTML вручную
   ↓ 100% успешность
```

---

## 🎯 Архитектура

```
┌──────────────────────────────────────────────┐
│  Frontend (CatalogDropdown)                   │
│  - Кнопка "Поиск по ссылке" 🌐               │
│  - Ввод URL                                   │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────┐
│  API Route: /api/catalog/search-by-url       │
│  - Валидация URL                              │
│  - Выбор стратегии парсинга                   │
└────────────────┬─────────────────────────────┘
                 │
         ┌───────┴────────┐
         ▼                ▼
┌─────────────────┐  ┌─────────────────┐
│ ClaudeWebFetch  │  │ UrlParserService│
│ Service         │  │ (Playwright)    │
│                 │  │                 │
│ - parseProduct  │  │ - parseWith     │
│   Url()         │  │   Playwright()  │
│                 │  │                 │
│ 0.27₽/запрос    │  │ Бесплатно       │
│ 30% success     │  │ 20% success     │
└─────────────────┘  └─────────────────┘
         │                │
         └────────┬───────┘
                  ▼
        ┌──────────────────┐
        │ Extracted        │
        │ Metadata:        │
        │ - title          │
        │ - brand          │
        │ - category       │
        │ - keywords       │
        └────────┬─────────┘
                 ▼
        ┌──────────────────┐
        │ Database Search  │
        │ (Supabase)       │
        │ - ILIKE query    │
        │ - 20 results max │
        └──────────────────┘
```

---

## 💰 Стоимость

### Claude Haiku 4.5:
- **Input tokens:** $1 за 1M токенов
- **Output tokens:** $5 за 1M токенов

### Средний запрос:
- Input: ~500 токенов (промпт + URL)
- Output: ~300 токенов (JSON ответ)
- **Стоимость:** ~$0.003 = **0.27₽**

### Примеры:
- 10 запросов/день = 2.70₽/день = **81₽/мес**
- 100 запросов/день = 27₽/день = **810₽/мес**
- 1000 запросов/день = 270₽/день = **8,100₽/мес**

**С учетом $5 бесплатных кредитов:**
Первые **18,500 запросов БЕСПЛАТНО!**

---

## 🔧 Отладка

### Проблема: "ANTHROPIC_API_KEY не найден"

**Решение:**
```bash
# Проверь что .env.local существует
ls -la .env.local

# Проверь содержимое
cat .env.local | grep ANTHROPIC

# Перезапусти dev сервер
npm run dev
```

### Проблема: "401 Unauthorized"

**Причина:** Неправильный API ключ

**Решение:**
1. Проверь что ключ начинается с `sk-ant-`
2. Убедись что скопировал без пробелов
3. Создай новый ключ на https://console.anthropic.com/

### Проблема: "Claude заблокирован на всех сайтах"

**Причина:** Сайты защищены Cloudflare/anti-bot

**Решение:**
1. Это нормально для Ozon/Wildberries
2. Попробуй простые сайты (Habr, новости)
3. Для защищенных нужен ScraperAPI

---

## 📈 Мониторинг использования

### Проверка расхода кредитов:

1. Зайди на https://console.anthropic.com/
2. Settings → Usage
3. Смотри:
   - Requests made
   - Tokens used
   - Credits remaining

### Логи в консоли:

```bash
# Запусти dev сервер с детальными логами
npm run dev

# В другом терминале делай запросы
curl -X POST http://localhost:3000/api/catalog/search-by-url \
  -H "Content-Type: application/json" \
  -d '{"url": "https://example.com"}'

# Смотри логи:
🤖 [Claude Web Fetch] Начинаем парсинг: https://example.com
✅ [Claude Web Fetch] Ответ получен
📄 [Claude Web Fetch] Ответ Claude: {...}
🎯 [Claude Web Fetch] Результат анализа: {...}
```

---

## 🚀 Следующие шаги

### Если Claude Web Fetch работает хорошо (>50% success):
✅ Оставляем как есть
✅ Мониторим расход кредитов
✅ При необходимости пополняем баланс

### Если Claude блокируется на нужных сайтах:
1. **Вариант A:** Добавить ScraperAPI для Ozon/WB ($49/мес)
2. **Вариант B:** Улучшить Playwright Stealth (бесплатно, но менее надежно)
3. **Вариант C:** Оставить ручной ввод HTML для защищенных сайтов

---

## 📚 Полезные ссылки

- [Claude Web Fetch документация](https://docs.anthropic.com/en/docs/agents-and-tools/tool-use/web-fetch-tool)
- [Anthropic Console](https://console.anthropic.com/)
- [Claude API Pricing](https://www.anthropic.com/pricing)
- [Тестовый скрипт](../scripts/test-claude-web-fetch.js)
- [ClaudeWebFetchService код](../lib/services/ClaudeWebFetchService.ts)

---

**Дата создания:** 04.11.2025
**Версия:** 1.0
**Статус:** ✅ Готово к тестированию
