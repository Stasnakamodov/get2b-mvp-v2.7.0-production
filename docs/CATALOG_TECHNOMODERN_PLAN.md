# 🚀 ПЛАН: Каталог в стиле TechnoModern

## Цель
Создать единый B2B каталог товаров с 10,000+ позициями, убрав разделение на "комнаты" (синяя/оранжевая → админка).

---

## 📐 АРХИТЕКТУРА

### Текущая структура (УБИРАЕМ)
```
┌─────────────────────────────────────────┐
│  🔵 Синяя комната (личные поставщики)   │
│  🟠 Оранжевая комната (аккредитованные) │
│  ↓ Модалки в конструкторе               │
└─────────────────────────────────────────┘
```

### Новая структура (TechnoModern)
```
┌─────────────────────────────────────────────────────────────┐
│                    🛒 ЕДИНЫЙ КАТАЛОГ                        │
├─────────────────────────────────────────────────────────────┤
│  НАВИГАЦИЯ:                                                 │
│  Категории → Подкатегории → Товары → Карточка товара        │
├─────────────────────────────────────────────────────────────┤
│  ФИЛЬТРЫ:                                                   │
│  [Поиск] [Категория] [Цена от-до] [Страна] [В наличии]     │
├─────────────────────────────────────────────────────────────┤
│  СОРТИРОВКА:                                                │
│  Популярные | Новые | Цена ↑ | Цена ↓ | Рейтинг            │
├─────────────────────────────────────────────────────────────┤
│  ОТОБРАЖЕНИЕ:                                               │
│  [Сетка 4x4] [Сетка 3x3] [Список]                          │
├─────────────────────────────────────────────────────────────┤
│  ТОВАРЫ:                                                    │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐                               │
│  │ 📦 │ │ 📦 │ │ 📦 │ │ 📦 │  ← Виртуализация             │
│  └────┘ └────┘ └────┘ └────┘    Infinite Scroll            │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐                               │
│  │ 📦 │ │ 📦 │ │ 📦 │ │ 📦 │                               │
│  └────┘ └────┘ └────┘ └────┘                               │
├─────────────────────────────────────────────────────────────┤
│  ПАГИНАЦИЯ:                                                 │
│  ← Предыдущая | Страница 1 из 200 | Следующая →            │
│  Загружено: 1,483 из 10,000 товаров                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 СТРУКТУРА ФАЙЛОВ

```
app/
├── dashboard/
│   └── catalog/                          # Новый каталог
│       ├── page.tsx                      # Главная страница каталога
│       ├── layout.tsx                    # Layout с sidebar категорий
│       ├── loading.tsx                   # Skeleton загрузки
│       ├── [categorySlug]/               # Динамическая категория
│       │   ├── page.tsx                  # Товары категории
│       │   └── [productId]/              # Карточка товара
│       │       └── page.tsx
│       └── components/
│           ├── CatalogHeader.tsx         # Поиск + фильтры
│           ├── CatalogSidebar.tsx        # Дерево категорий
│           ├── CatalogGrid.tsx           # Сетка товаров (виртуализация)
│           ├── ProductCard.tsx           # Карточка товара в сетке
│           ├── ProductDetail.tsx         # Полная карточка товара
│           ├── FilterPanel.tsx           # Панель фильтров
│           ├── SortDropdown.tsx          # Сортировка
│           ├── ViewToggle.tsx            # Переключатель вида
│           ├── CategoryBreadcrumb.tsx    # Хлебные крошки
│           ├── AddToProjectButton.tsx    # Кнопка "Добавить в проект"
│           └── CartPreview.tsx           # Превью корзины/проекта
│
├── api/
│   └── catalog/
│       ├── products-paginated/route.ts   # ✅ Уже создан
│       ├── categories-tree/route.ts      # Дерево категорий
│       ├── product/[id]/route.ts         # Детали товара
│       ├── filters/route.ts              # Доступные фильтры
│       └── search/route.ts               # Полнотекстовый поиск
│
components/
├── catalog/
│   ├── VirtualProductList.tsx            # ✅ Уже создан
│   ├── VirtualProductGrid.tsx            # Новый: сетка с виртуализацией
│   ├── ProductCardCompact.tsx            # Компактная карточка
│   ├── ProductCardFull.tsx               # Полная карточка
│   └── CategoryTree.tsx                  # Дерево категорий
│
hooks/
├── useInfiniteProducts.ts                # ✅ Уже создан
├── useCatalogFilters.ts                  # Новый: управление фильтрами
├── useCatalogSearch.ts                   # Новый: поиск с debounce
└── useProductCart.ts                     # Новый: корзина товаров

lib/
└── catalog/
    ├── types.ts                          # Типы каталога
    ├── constants.ts                      # Константы (категории, фильтры)
    └── utils.ts                          # Утилиты (форматирование цен)
```

---

## 🗄️ БАЗА ДАННЫХ

### Текущие таблицы (ОСТАВЛЯЕМ)
```sql
-- Основная таблица товаров
catalog_verified_products (
  id, supplier_id, name, description, category,
  sku, price, currency, min_order, in_stock,
  specifications, images, is_active, created_at
)

-- Поставщики
catalog_verified_suppliers (
  id, name, company_name, category, country,
  contact_email, payment_methods, rating, is_active
)

-- Категории
catalog_categories (
  id, key, name, icon, description, parent_id,
  has_subcategories, sort_order, is_active
)

-- Подкатегории
catalog_subcategories (
  id, category_id, name, key
)
```

### Новые индексы для производительности
```sql
-- Полнотекстовый поиск
CREATE INDEX idx_products_search ON catalog_verified_products
USING GIN (to_tsvector('russian', name || ' ' || COALESCE(description, '')));

-- Составной индекс для фильтрации
CREATE INDEX idx_products_filter ON catalog_verified_products
(category, is_active, price) WHERE is_active = true;

-- Индекс для сортировки
CREATE INDEX idx_products_sort ON catalog_verified_products
(created_at DESC, price ASC) WHERE is_active = true;
```

### RPC функция для каталога
```sql
CREATE OR REPLACE FUNCTION get_catalog_products(
  p_category TEXT DEFAULT NULL,
  p_search TEXT DEFAULT NULL,
  p_min_price DECIMAL DEFAULT NULL,
  p_max_price DECIMAL DEFAULT NULL,
  p_in_stock BOOLEAN DEFAULT NULL,
  p_sort_by TEXT DEFAULT 'created_at',
  p_sort_order TEXT DEFAULT 'desc',
  p_limit INT DEFAULT 50,
  p_cursor TEXT DEFAULT NULL
)
RETURNS TABLE (
  id UUID,
  name TEXT,
  description TEXT,
  category TEXT,
  price DECIMAL,
  currency TEXT,
  images JSONB,
  supplier_name TEXT,
  supplier_country TEXT,
  in_stock BOOLEAN,
  min_order TEXT,
  created_at TIMESTAMPTZ,
  next_cursor TEXT
)
LANGUAGE plpgsql
AS $$
BEGIN
  -- Реализация с keyset pagination
  -- Полнотекстовый поиск
  -- Фильтрация
  -- Сортировка
END;
$$;
```

---

## 🎨 КОМПОНЕНТЫ UI

### 1. CatalogHeader (Шапка каталога)
```
┌─────────────────────────────────────────────────────────────────┐
│ 🔍 [_________Поиск товаров..._________]  [🔽 Категория]         │
│                                                                 │
│ Фильтры: [Цена ▼] [Страна ▼] [✓ В наличии] [Сбросить]          │
│                                                                 │
│ Сортировка: [Популярные ▼]     Вид: [▦] [≡] [☰]    📦 1,483    │
└─────────────────────────────────────────────────────────────────┘
```

### 2. CatalogSidebar (Боковое меню)
```
┌────────────────────┐
│ 📂 КАТЕГОРИИ       │
├────────────────────┤
│ ▼ Электроника (523)│
│   ├─ Смартфоны     │
│   ├─ Ноутбуки      │
│   └─ Аксессуары    │
│ ▶ Дом и быт (730)  │
│ ▶ Автотовары (82)  │
│ ▶ Текстиль (177)   │
│ ▶ Строительство    │
└────────────────────┘
```

### 3. ProductCard (Карточка товара)
```
┌─────────────────────┐
│  ┌───────────────┐  │
│  │               │  │
│  │    📷 IMG     │  │
│  │               │  │
│  └───────────────┘  │
│                     │
│  Название товара    │
│  в 2 строки макс... │
│                     │
│  💰 569.40 ₽        │
│  📦 Мин: 3 шт       │
│                     │
│  🏢 TechnoModern    │
│  🇨🇳 Китай          │
│                     │
│  [+ В проект]       │
└─────────────────────┘
```

### 4. ProductDetail (Детальная карточка)
```
┌────────────────────────────────────────────────────────────────┐
│ ← Назад в каталог                                              │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌──────────────────┐    НАЗВАНИЕ ТОВАРА                       │
│  │                  │    ★★★★☆ (4.5) · 127 отзывов             │
│  │                  │                                          │
│  │    ГАЛЕРЕЯ       │    💰 569.40 ₽                           │
│  │    ИЗОБРАЖЕНИЙ   │    📦 Минимальный заказ: 3 шт            │
│  │                  │    ✅ В наличии                          │
│  │                  │                                          │
│  └──────────────────┘    ─────────────────────────             │
│  [○] [○] [●] [○]         Описание товара...                    │
│                          ...максимум 3-4 строки...             │
│                                                                │
│  ─────────────────────────────────────────────────────────     │
│  ХАРАКТЕРИСТИКИ                                                │
│  Артикул: 580565159746                                         │
│  Категория: Электроника                                        │
│  Продавец: TechnoModern Import                                 │
│  Страна: Китай, Гуандун                                        │
│  ─────────────────────────────────────────────────────────     │
│                                                                │
│  Количество: [-] 3 [+]                                         │
│                                                                │
│  [        🛒 ДОБАВИТЬ В ПРОЕКТ        ]                        │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 🔄 API ENDPOINTS

### 1. GET /api/catalog/products-paginated ✅ (Создан)
```typescript
// Query params:
cursor?: string       // Курсор для пагинации
limit?: number        // Количество (default: 50, max: 100)
category?: string     // Фильтр по категории
search?: string       // Поисковый запрос
min_price?: number    // Мин. цена
max_price?: number    // Макс. цена
in_stock?: boolean    // Только в наличии
sort_by?: string      // Поле сортировки
sort_order?: string   // asc | desc
```

### 2. GET /api/catalog/categories-tree (Новый)
```typescript
// Response:
{
  categories: [
    {
      id: string,
      name: string,
      icon: string,
      slug: string,
      productsCount: number,
      children: [
        { id, name, slug, productsCount }
      ]
    }
  ]
}
```

### 3. GET /api/catalog/product/[id] (Новый)
```typescript
// Response:
{
  product: {
    id, name, description, category,
    price, currency, images,
    specifications: { key: value },
    supplier: { id, name, country, rating },
    relatedProducts: [...],
    reviews: [...]
  }
}
```

### 4. GET /api/catalog/filters (Новый)
```typescript
// Response:
{
  categories: [...],
  priceRange: { min: 0, max: 100000 },
  countries: ['Китай', 'Турция', ...],
  suppliers: [...]
}
```

### 5. GET /api/catalog/search (Новый)
```typescript
// Query: q=смартфон&limit=20
// Response:
{
  products: [...],
  suggestions: ['смартфон xiaomi', 'смартфон samsung'],
  totalCount: 1234
}
```

---

## 🎯 ФИЧИ

### Phase 1: Базовый каталог
- [ ] Страница каталога с сеткой товаров
- [ ] Виртуализация списка (TanStack Virtual)
- [ ] Infinite scroll с cursor-based пагинацией
- [ ] Базовая фильтрация по категории
- [ ] Поиск с debounce
- [ ] Карточка товара (компактная)

### Phase 2: Расширенная фильтрация
- [ ] Боковое меню с деревом категорий
- [ ] Фильтр по цене (range slider)
- [ ] Фильтр по стране поставщика
- [ ] Фильтр "В наличии"
- [ ] Сохранение фильтров в URL
- [ ] Сортировка (цена, дата, популярность)

### Phase 3: Детальная карточка
- [ ] Страница товара /catalog/[productId]
- [ ] Галерея изображений
- [ ] Характеристики товара
- [ ] Информация о поставщике
- [ ] Похожие товары
- [ ] Кнопка "Добавить в проект"

### Phase 4: Корзина и интеграция
- [ ] Корзина товаров (localStorage + state)
- [ ] Превью корзины в header
- [ ] Переход в конструктор проекта с товарами
- [ ] Автозаполнение шага 2 (спецификация)

### Phase 5: Админка (отдельно)
- [ ] Разделение синяя/оранжевая комната
- [ ] Модерация поставщиков
- [ ] Управление товарами
- [ ] Импорт из OTAPI

---

## 📊 ОПТИМИЗАЦИЯ ДЛЯ 10,000+ ТОВАРОВ

### 1. Виртуализация (TanStack Virtual)
```typescript
// Рендерим только видимые элементы
const virtualizer = useVirtualizer({
  count: products.length,
  getScrollElement: () => parentRef.current,
  estimateSize: () => 280, // Высота карточки
  overscan: 5
})
```

### 2. Cursor-based пагинация
```typescript
// Вместо offset/limit используем курсор
// Стабильная производительность на любой странице
const { nextCursor } = await fetchProducts({ cursor: lastCursor })
```

### 3. Кэширование (React Query)
```typescript
// Кэш на 5 минут, фоновое обновление
const { data } = useInfiniteQuery({
  staleTime: 5 * 60 * 1000,
  gcTime: 10 * 60 * 1000
})
```

### 4. Debounce поиска
```typescript
// 300ms задержка перед запросом
const debouncedSearch = useDebouncedValue(search, 300)
```

### 5. Lazy loading изображений
```typescript
// Next.js Image с приоритетом для видимых
<Image loading="lazy" placeholder="blur" />
```

### 6. Серверный поиск
```typescript
// Полнотекстовый поиск в PostgreSQL
WHERE to_tsvector('russian', name) @@ plainto_tsquery('russian', $1)
```

---

## 🔗 ИНТЕГРАЦИЯ С КОНСТРУКТОРОМ

### Сценарий использования
```
1. Пользователь открывает каталог
2. Выбирает товары, добавляет в корзину
3. Нажимает "Создать проект"
4. Переходит в конструктор с заполненным шагом 2
```

### Передача данных
```typescript
// URL параметры
/dashboard/project-constructor?fromCatalog=true&cartId=xxx

// Или через localStorage
localStorage.setItem('catalogCart', JSON.stringify(cartItems))

// В конструкторе
const cartItems = JSON.parse(localStorage.getItem('catalogCart'))
setManualData(prev => ({
  ...prev,
  2: {
    items: cartItems.map(item => ({
      item_name: item.name,
      quantity: item.quantity,
      price: item.price,
      unit: 'шт',
      total: item.price * item.quantity,
      supplier_name: item.supplierName,
      supplier_id: item.supplierId
    })),
    total_amount: cartItems.reduce((sum, item) => sum + item.price * item.quantity, 0),
    currency: 'RUB',
    source: 'catalog'
  }
}))
```

---

## 📅 ПЛАН РЕАЛИЗАЦИИ

### День 1-2: Базовый каталог
1. Создать страницу /dashboard/catalog (новую)
2. Компонент CatalogGrid с виртуализацией
3. Компонент ProductCard
4. Интеграция с существующим API

### День 3: Фильтрация и поиск
1. CatalogHeader с поиском
2. FilterPanel с фильтрами
3. URL state для фильтров
4. API для фильтров

### День 4: Категории
1. CatalogSidebar с деревом
2. API categories-tree
3. Breadcrumb навигация
4. Подкатегории

### День 5: Детальная карточка
1. Страница /catalog/[id]
2. Галерея изображений
3. Характеристики
4. Похожие товары

### День 6: Корзина
1. useProductCart хук
2. CartPreview компонент
3. Интеграция с конструктором
4. Автозаполнение шага 2

### День 7: Полировка
1. Skeleton загрузки
2. Error boundaries
3. Мобильная адаптация
4. Тестирование

---

## 🎨 СТИЛИЗАЦИЯ (TailwindCSS)

### Цветовая схема
```css
/* Основной акцент */
--color-primary: theme('colors.orange.500')  /* #f97316 */
--color-primary-hover: theme('colors.orange.600')

/* Фоны */
--color-bg-card: theme('colors.white')
--color-bg-hover: theme('colors.orange.50')
--color-bg-selected: theme('colors.orange.100')

/* Текст */
--color-text-primary: theme('colors.gray.900')
--color-text-secondary: theme('colors.gray.600')
--color-text-muted: theme('colors.gray.400')

/* Границы */
--color-border: theme('colors.gray.200')
--color-border-hover: theme('colors.orange.400')
```

### Анимации
```css
/* Hover карточки */
.product-card {
  @apply transition-all duration-200;
  @apply hover:shadow-lg hover:border-orange-400 hover:-translate-y-1;
}

/* Skeleton */
.skeleton {
  @apply animate-pulse bg-gray-200 rounded;
}
```

---

## ✅ ГОТОВЫЕ КОМПОНЕНТЫ (использовать)

1. `VirtualProductList` - виртуализированный список ✅
2. `useInfiniteProducts` - хук для infinite scroll ✅
3. `QueryProvider` - React Query провайдер ✅
4. `/api/catalog/products-paginated` - API с пагинацией ✅

---

## 🚫 УБРАТЬ/ПЕРЕМЕСТИТЬ В АДМИНКУ

1. `OrangeRoomSupplierModal` → Админка модерации
2. `BlueRoomSupplierModal` → Админка личных поставщиков
3. Разделение verified/user suppliers → Только для админов
4. `catalogSourceStep` в конструкторе → Упростить

---

## КОМАНДА ДЛЯ СТАРТА

```bash
# Создать базовую структуру
mkdir -p app/dashboard/catalog-new/components
mkdir -p app/dashboard/catalog-new/[categorySlug]
mkdir -p app/dashboard/catalog-new/[categorySlug]/[productId]

# Запустить dev сервер
npm run dev

# Открыть новый каталог
open http://localhost:3002/dashboard/catalog-new
```
