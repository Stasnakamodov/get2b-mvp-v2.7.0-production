# Поиск товаров по URL - Итоговая документация

## 📋 Статус реализации

**✅ Полностью реализовано:**
- Backend API endpoint `/api/catalog/search-by-url`
- UrlParserService с поддержкой 6+ маркетплейсов
- YandexGPTService с анализом метаданных товаров
- UI кнопка глобуса в строке поиска
- Модальное окно для ввода URL
- База данных с тестовыми товарами (6 тормозных жидкостей)

**⚠️ Ограничения:**
- Маркетплейсы (Ozon, Wildberries) имеют защиту от парсинга
- Требуется решение для production (см. ниже)

---

## 🏗️ Архитектура

### 1. Frontend (CatalogDropdown.tsx)

```typescript
// Кнопка глобуса в строке поиска (line 629-638)
<button
  onClick={() => setIsUrlSearchOpen(true)}
  className="absolute right-20 top-1/2 -translate-y-1/2..."
  title="Поиск по ссылке из интернета"
>
  <Globe className="h-5 w-5 text-white" />
</button>

// Обработчик поиска (line 326-389)
const handleUrlSearch = async () => {
  const response = await fetch('/api/catalog/search-by-url', {
    method: 'POST',
    body: JSON.stringify({ url: searchUrl })
  })

  // Переход в каталог с результатами
  router.push(`/dashboard/catalog?category=${productCategory}&view=products`)
}
```

### 2. Backend API (`app/api/catalog/search-by-url/route.ts`)

```typescript
POST /api/catalog/search-by-url
Body: { "url": "https://..." }

// Поток работы:
1. Парсинг URL → metadata (название, описание, маркетплейс)
2. YandexGPT анализ → analysis (бренд, категория, ключевые слова)
3. Формирование поисковых терминов
4. Поиск в БД по ILIKE
5. Возврат: metadata + analysis + products[]
```

### 3. UrlParserService (`lib/services/UrlParserService.ts`)

```typescript
export class UrlParserService {
  async parseProductUrl(url: string): Promise<ParsedProductMetadata>

  // Методы:
  - detectMarketplace() // wildberries, ozon, aliexpress, yandex, amazon
  - parseHtml() // прямой HTTP запрос
  - parseOGFromHTML() // Open Graph теги из HTML
  - parseWildberries() // специфичный парсер
  - parseOzon() // специфичный парсер
  ...
}
```

**Поддерживаемые маркетплейсы:**
- Wildberries (`*.wildberries.ru`)
- Ozon (`*.ozon.ru`)
- AliExpress (`*.aliexpress.ru/com`)
- Яндекс.Маркет (`market.yandex.ru`)
- СберМегаМаркет (`*.sbermegamarket.ru`)
- Amazon (`*.amazon.*`)

### 4. YandexGPTService расширение

```typescript
async analyzeProductFromMetadata(
  title: string,
  description: string,
  marketplace?: string
): Promise<ProductAnalysis>

// Возвращает:
{
  brand: "ЛУКОЙЛ",
  category: "Автотовары",
  productType: "Тормозная жидкость",
  keywords: ["ЛУКОЙЛ", "DOT-3", "тормозная жидкость", "brake fluid", ...],
  description: "Тормозная жидкость ЛУКОЙЛ DOT 3"
}
```

---

## 🚧 Проблема с парсингом маркетплейсов

### Причина

Современные маркетплейсы имеют многоуровневую защиту от автоматического парсинга:

1. **Бесконечные редиректы** - Ozon создает цикл перенаправлений
2. **JavaScript rendering** - контент генерируется на клиенте
3. **Bot detection** - анализ поведения, fingerprinting
4. **Rate limiting** - блокировка по IP
5. **CAPTCHA** - требование прохождения проверки

### Тестирование показало

```bash
$ node scripts/test-direct-fetch.js
# ❌ Ошибка: redirect count exceeded
# Причина: Ozon перенаправляет на страницу проверки бота

$ node scripts/test-ozon-parse.js
# ❌ Open Graph error: redirect count exceeded
# Причина: библиотека open-graph-scraper тоже не проходит защиту
```

---

## ✅ Решения для Production

### Вариант 1: Browser Automation (Рекомендуется для MVP)

**Puppeteer / Playwright:**

```javascript
import puppeteer from 'puppeteer'

async function parseWithBrowser(url: string) {
  const browser = await puppeteer.launch({
    headless: 'new',
    args: ['--no-sandbox', '--disable-setuid-sandbox']
  })

  const page = await browser.newPage()
  await page.setUserAgent('Mozilla/5.0...')
  await page.goto(url, { waitUntil: 'networkidle2' })

  // Извлекаем данные
  const metadata = await page.evaluate(() => ({
    title: document.querySelector('meta[property="og:title"]')?.content,
    description: document.querySelector('meta[property="og:description"]')?.content,
    price: document.querySelector('[data-price]')?.textContent
  }))

  await browser.close()
  return metadata
}
```

**Pros:**
- Обходит большинство защит
- Работает с JS-rendered контентом
- Можно обрабатывать CAPTCHA

**Cons:**
- Медленнее (2-5 секунд на запрос)
- Требует больше ресурсов (RAM, CPU)
- Стоимость: 10-20₽/месяц на VPS

---

### Вариант 2: Proxy + User-Agent Rotation

**Использование rotating proxies:**

```javascript
import axios from 'axios'

const proxies = [
  'http://proxy1.com:8000',
  'http://proxy2.com:8000',
  ...
]

async function parseWithProxy(url: string) {
  const proxy = proxies[Math.floor(Math.random() * proxies.length)]

  const response = await axios.get(url, {
    proxy: { host: proxy.split(':')[0], port: proxy.split(':')[1] },
    headers: { 'User-Agent': getRandomUserAgent() }
  })

  return parseHTML(response.data)
}
```

**Pros:**
- Быстрее browser automation
- Обходит IP блокировки

**Cons:**
- Платные прокси (от 500₽/месяц)
- Может не работать с JS контентом
- Требует регулярного обновления User-Agents

---

### Вариант 3: API интеграции маркетплейсов

**Официальные API:**

- **Ozon Seller API** - требует регистрации как продавец
- **Wildberries API** - только для партнеров
- **AliExpress API** - требует бизнес-аккаунт

**Pros:**
- Легальный способ
- Стабильные данные
- Нет блокировок

**Cons:**
- Требует регистрации и верификации
- Доступ не ко всем товарам
- Лимиты запросов

---

### Вариант 4: Сторонние сервисы парсинга

**Готовые решения:**

- **ScraperAPI** (scraperapi.com) - от $49/месяц
- **Bright Data** (brightdata.com) - от $500/месяц
- **Oxylabs** (oxylabs.io) - enterprise

```javascript
const response = await fetch('http://api.scraperapi.com', {
  params: {
    api_key: 'YOUR_KEY',
    url: 'https://www.ozon.ru/...',
    render: 'true' // JS rendering
  }
})
```

**Pros:**
- Готовое решение "из коробки"
- Обходят все защиты
- Масштабируемость

**Cons:**
- Высокая стоимость
- Зависимость от сервиса

---

## 🎯 Рекомендация для вашего проекта

### Краткосрочно (MVP):

**Вариант 1 + Fallback:**

```typescript
async parseProductUrl(url: string) {
  try {
    // 1. Пробуем простой HTTP (работает для мелких сайтов)
    return await this.parseWithFetch(url)
  } catch {
    try {
      // 2. Fallback на Puppeteer (для защищенных сайтов)
      return await this.parseWithBrowser(url)
    } catch {
      // 3. Если всё не работает - предлагаем ручной ввод
      throw new Error('Не удалось автоматически распарсить. Введите данные вручную.')
    }
  }
}
```

**Стоимость:** ~500₽/месяц (VPS для Puppeteer)

### Долгосрочно (Scale):

1. **Официальные API** для основных маркетплейсов
2. **ScraperAPI** для остальных сайтов
3. **Кэширование** результатов на 24 часа

**Стоимость:** ~$100/месяц при 1000 запросов/день

---

## 📊 Текущая реализация - Что работает

### ✅ Готово к использованию:

1. **UI полностью рабочий:**
   - Кнопка глобуса в строке поиска
   - Модальное окно для ввода URL
   - Обработка результатов
   - Переход в каталог

2. **Backend инфраструктура:**
   - API endpoint работает
   - YandexGPT интеграция
   - Поиск в базе данных
   - Логирование

3. **База данных:**
   - 6 тестовых товаров (тормозные жидкости)
   - Поставщик "АвтоХимПром"

### 🔧 Что можно протестировать сейчас:

**Опция A: Ручной ввод данных**

Добавить в UI возможность ручного ввода названия товара вместо URL:

```typescript
// В модальном окне:
<input
  placeholder="Введите название товара или вставьте ссылку"
  onChange={(e) => {
    if (isURL(e.target.value)) {
      // Автоматический парсинг
      handleUrlSearch()
    } else {
      // Обычный текстовый поиск
      handleTextSearch(e.target.value)
    }
  }}
/>
```

**Опция B: Демо-режим**

Распознавать тестовые URL и возвращать мок-данные:

```typescript
// В UrlParserService:
if (url.includes('demo') || url.includes('test')) {
  return {
    title: "Тормозная жидкость ЛУКОЙЛ DOT 3",
    description: "Для гидравлических тормозных систем...",
    marketplace: "demo"
  }
}
```

---

## 🧪 Тестирование

### Добавлены тестовые скрипты:

```bash
# Добавить тестовые товары в БД
node scripts/add-brake-fluid-supplier.js

# Тестировать API
node scripts/test-url-search.js

# Тестировать парсинг Ozon
node scripts/test-ozon-parse.js

# Тестировать прямой HTTP
node scripts/test-direct-fetch.js
```

### Тестовые данные в БД:

**Поставщик:** АвтоХимПром
**Товары:**
1. Тормозная жидкость ЛУКОЙЛ DOT 3 - 314₽
2. Тормозная жидкость G-Energy Expert DOT 4 - 425₽
3. Тормозная жидкость SINTEC SUPER DOT-4 - 257₽
4. Тормозная жидкость ЛУКОЙЛ DOT 4 - 351₽
5. Тормозная жидкость Felix DOT 4 - 189₽
6. Тормозная жидкость Rosdot DOT 4 - 295₽

---

## 📝 Итоги

### Что сделано:
✅ Полная архитектура поиска по URL
✅ UI с модальным окном
✅ Backend API endpoint
✅ UrlParserService с поддержкой 6+ маркетплейсов
✅ YandexGPT интеграция
✅ Тестовые данные в БД
✅ Подробная документация

### Что нужно для production:
⚠️ Puppeteer/Playwright для обхода защиты
⚠️ Прокси или ScraperAPI
⚠️ Или официальные API маркетплейсов

### Следующие шаги:

1. **Решить про парсинг:**
   - Установить Puppeteer
   - Или использовать ScraperAPI
   - Или добавить ручной ввод как fallback

2. **Тестирование:**
   - Протестировать на сайтах без защиты
   - Проверить YandexGPT анализ

3. **Оптимизация:**
   - Добавить кэширование результатов
   - Rate limiting для API

---

## 🎬 Демо использования (когда заработает парсинг)

1. Пользователь заходит на http://localhost:3000/dashboard
2. Нажимает кнопку глобуса (🌐) в строке поиска
3. Вставляет ссылку: `https://www.ozon.ru/product/...`
4. Система:
   - Парсит название "Тормозная жидкость ЛУКОЙЛ DOT 3"
   - YandexGPT извлекает: бренд="ЛУКОЙЛ", категория="Автотовары", ключевые слова
   - Ищет в БД похожие товары
5. Показывает результаты или предлагает оставить заявку

---

**Документация создана:** 31.10.2025
**Версия:** 1.0
**Статус:** Ready for review
